---
title: SIMULA Evidence Ledger
status: active
created: 2026-07-17
updated: 2026-07-30
owner: Principal program and engineering lead
classification: OBSERVED
source_of_truth: true
---

# SIMULA Evidence Ledger

## Classification and use

Labels: OBSERVED = directly inspected source; REPORTED = source or study claim; INFERRED = bounded synthesis; UNKNOWN = evidence gap; PROPOSED = SIMULA decision. Every external row includes source, date, access date, confidence, limitation, and destination. Vendor evidence proves publication of a claim, not its truth.

## Internal authority

| ID | Source | Date | Accessed | Evidence | Class | Confidence | Limitation | Used in |
|---|---|---:|---:|---|---|---|---|---|
| E-0001 | [AGENT.md](../AGENT.md) | Unknown | 2026-07-17 | Repository contract defines evidence, architecture, security, testing, and Obsidian requirements. | OBSERVED | High | Internal authority, not market evidence. | All notes |
| E-0002 | [.agent/PLANS.md](../.agent/PLANS.md) | Unknown | 2026-07-17 | Defines required ExecPlan structure and validation evidence. | OBSERVED | High | Internal process standard. | Plans |
| E-0003 | [BOOTSTRAP_PROMPT.md](../BOOTSTRAP_PROMPT.md) | Unknown | 2026-07-17 | Defines product mission, constraints, phase gates, and required artifacts. | OBSERVED | High | Product brief, not external validation. | Roadmap, requirements |

## Competitor and market evidence

| ID | Source | Published | Accessed | Evidence | Class | Confidence | Limitation | Used in |
|---|---|---:|---:|---|---|---|---|---|
| E-1001 | [Predikta](https://predikta.ai/) — Predikta AI | n.d.; page ©2026 | 2026-07-17 | Public site positions a pre-launch campaign simulator for marketers and exposes audience, simulation, sentiment, risk, and recommendation workflow. | OBSERVED | High | Public marketing surface; authenticated behavior untested. | Predikta teardown, journeys |
| E-1002 | [Predikta](https://predikta.ai/) — Predikta AI | n.d.; page ©2026 | 2026-07-17 | Vendor claims modeled coverage of 70 million Filipinos and peer-reviewed or validated psychological grounding. | REPORTED | Medium | No public population-frame audit or independent replication located. | Predikta teardown, risks |
| E-1003 | [Predikta Marketing](https://app.predikta.ai/) — Predikta AI | n.d. | 2026-07-17 | Public application entry and sign-up surfaces exist. | OBSERVED | High | No authenticated workflow was exercised. | Predikta teardown |
| E-1004 | [Participant Information Sheet](https://survey.predikta.ai/participant-information-sheet/) — Predikta/Netopia AI | n.d.; footer ©2024 | 2026-07-17 | Vendor says one research survey collects personality, opinions, beliefs, behavior, and demographics for product development and validation, with stated participant rights. | REPORTED | High | Applies to that survey only; not proof of every product dataset or control. | Predikta teardown, privacy |
| E-1005 | [Sentiment Simulation using Generative AI Agents](https://arxiv.org/abs/2505.22125) — Tia et al., arXiv | 2025-05-28 | 2026-07-17 | Authors describe 2,485 Filipino survey respondents and report task-specific response, sentiment, stability, and framing results. | REPORTED | Medium | Preprint; author/vendor overlap; no independent replication found. | Teardowns, methodology |
| E-1006 | [917Ventures’ Velocity Program Helps Predikta Prove AI Can Forecast Ad Performance](https://www.globe.com.ph/about-us/newsroom/corporate/917ventures-netopia-validate-predikta) — Globe | 2026-05-28 | 2026-07-17 | Partner newsroom reports blind comparison of Predikta scores with historical AdSpark outcomes. | REPORTED | Medium | Involved partner; public article lacks reproducible protocol and raw data. | Predikta teardown |
| E-1007 | [Predikta backtesting coverage](https://unbox.ph/news/predikta-adspark-backtesting-study/) — Unbox PH | 2026-06-03 | 2026-07-17 | Coverage limits current validation to copy-level use and says it does not replace live testing or predict exact KPIs. | REPORTED | Medium | Likely based on company materials; not independent replication. | Predikta teardown, non-goals |
| E-1008 | [Predikta signup](https://app.predikta.ai/signup) and [homepage](https://predikta.ai/) — Predikta AI | n.d. | 2026-07-17 | Inspected public surfaces showed legal-policy labels, but no policy text or public document URL was exposed in rendered markup; common policy routes were also checked. | UNKNOWN | High for inspected scope | A client-only, authenticated, or undiscovered route may exist; no conclusion about legal compliance. | Predikta teardown |
| E-1010 | [Netopia AI](https://www.netopia.ai/) — Netopia AI | n.d. | 2026-07-17 | Vendor says its behavioral model uses surveys, decision traces, choice records, and contextual signals. | REPORTED | Medium | No source-level provenance, sampling, license, or audit detail published. | Netopia teardown |
| E-1011 | [Aaru](https://aaru.com/) — Aaru | n.d. | 2026-07-17 | Vendor claims population simulation and partner validation. | REPORTED | Low | Public page lacks sufficient protocol and error detail. | Landscape |
| E-1012 | [Evidenza](https://www.evidenza.ai/) — Evidenza | n.d. | 2026-07-17 | Vendor reports synthetic/traditional similarity and customer correlations. | REPORTED | Low | Aggregate vendor metrics and hosted testimonials. | Landscape |
| E-1013 | [Synthetic Users](https://www.syntheticusers.com/) — Synthetic Users | n.d. | 2026-07-17 | Vendor markets synthetic qualitative research while explicitly retaining real-user validation. | REPORTED | Medium | Vendor-defined qualitative parity, not outcome prediction. | Landscape, positioning |
| E-1014 | [SyntheticAudience.ai](https://www.syntheticaudience.ai/) — BotsCrew | n.d. | 2026-07-17 | Vendor claims live-panel RCT validation, correlation thresholds, and drift monitoring. | REPORTED | Low | No public named study, raw results, or external audit located. | Landscape |
| E-1015 | [Foundation model to predict and capture human cognition](https://www.nature.com/articles/s41586-025-09215-4) — Binz et al., Nature | 2025-07-02 | 2026-07-17 | Peer-reviewed study reports broad cognitive-task prediction and flags WEIRD-population and individual-difference limits. | REPORTED | High | Cognitive experiments, not campaign simulation. | Landscape, methodology |
| E-1016 | [Simulating human well-being with large language models](https://www.pnas.org/doi/10.1073/pnas.2519394122) — Pataranutaporn et al., PNAS | 2025-11-26 | 2026-07-17 | Cross-country study reports systematic errors, especially in underrepresented countries, and rejects replacing direct self-report. | REPORTED | High | Well-being domain; not a Predikta product test. | Landscape, methodology |
| E-1017 | [Netopia AI](https://www.netopia.ai/) — Netopia AI | n.d. | 2026-07-17 | Public site presents Predikta as Netopia’s in-market campaign-simulation application. | OBSERVED | High | Product relationship/positioning only; no internal architecture implication. | Netopia teardown, matrix |
| E-1018 | [Netopia AI](https://www.netopia.ai/) and common public policy routes — Netopia AI | n.d. | 2026-07-17 | Main public surface displayed privacy/terms labels, but inspected rendered markup and common routes did not expose policy text. | UNKNOWN | High for inspected scope | Client-only, authenticated, or undiscovered documents may exist; no compliance conclusion. | Netopia teardown |
| E-1019 | [PhantomCrowd source](https://github.com/l2dnjsrud/PhantomCrowd/tree/4f197a8df0de5183f2376a210f42aaf948bd9b0a) and [MIT license](https://github.com/l2dnjsrud/PhantomCrowd/blob/4f197a8df0de5183f2376a210f42aaf948bd9b0a/LICENSE) | commit observed 2026-07-29 | 2026-07-29 | Exact revision implements a FastAPI/Vue campaign simulator with a context graph, tiered LLM/rule agents, interaction rounds/actions, agent memory, report tools, synthetic interviews, quick simulation, export, and A/B comparison. It uses SQLite/process-local state and contains permissive defaults and security patterns unsuitable for SIMULA production authority. | OBSERVED | High | Public source inspection proves the code present at this revision, not capability, scale, correctness, or safe hosted operation. MIT reuse requires preservation of its copyright and permission notice. | PhantomCrowd implementation reference, ADR-0012, Plan 003 |
| E-1020 | [PhantomCrowd validation report](https://github.com/l2dnjsrud/PhantomCrowd/blob/4f197a8df0de5183f2376a210f42aaf948bd9b0a/docs/validation-report.md) | repository document | 2026-07-29 | The document reports 50-campaign Pearson correlation 0.469, MAE 21.6, 71% directional accuracy, 30% exact-bucket accuracy, and 60% near-bucket accuracy; it also records score compression, context-insensitive scoring, and a cultural-sensitivity failure before prompt-anchor/penalty changes. | REPORTED | Medium | The inspected revision lacks the dataset file named by the report; sources, hand labels, leakage controls, controversy-mode mismatch, and independent replication remain unresolved. These metrics do not validate SIMULA or support population/KPI claims. | PhantomCrowd implementation reference, R-035, ADR-0012 |

## Methodology evidence

| ID | Source | Published | Accessed | Evidence | Class | Confidence | Limitation | Used in |
|---|---|---:|---:|---|---|---|---|---|
| E-2001 | [Creating Synthetic Baseline Populations](https://doi.org/10.1016/0965-8564(96)00004-3) — Beckman et al. | 1996-11 | 2026-07-17 | Aggregate controls and microdata can synthesize households; joint fidelity still needs validation. | REPORTED | High | Historical US transport case; no behavioral or privacy validity. | Methodology, data |
| E-2002 | [A Bayesian Network Approach for Population Synthesis](https://doi.org/10.1016/j.trc.2015.10.010) — Sun and Erath | 2015-12 | 2026-07-17 | Dependency models can represent joint structure missed by marginal fitting. | REPORTED | Medium | Singapore travel case; learned structure is dataset-specific. | Methodology |
| E-2003 | [Out of One, Many](https://doi.org/10.1017/pan.2023.2) — Argyle et al. | 2023-02-21 | 2026-07-17 | Demographically conditioned GPT-3 sometimes reproduced US survey patterns. | REPORTED | Medium | Historical model and US political tasks; no general replacement claim. | Methodology |
| E-2004 | [Whose Opinions Do Language Models Reflect?](https://proceedings.mlr.press/v202/santurkar23a.html) — Santurkar et al. | 2023-07 | 2026-07-17 | Tested models misaligned with many demographic groups; prompt steering did not remove all gaps. | REPORTED | High | US polling questions and specific model versions. | Validation, fairness |
| E-2005 | [Synthetic Replacements for Human Survey Data?](https://doi.org/10.1017/pan.2024.5) — Bisbee et al. | 2024-05-17 | 2026-07-17 | Study found variance compression, changed inference, prompt sensitivity, and temporal drift despite some mean agreement. | REPORTED | High | ChatGPT 3.5 and US political measures. | Validation, non-goals |
| E-2006 | [Strictly Proper Scoring Rules](https://doi.org/10.1198/016214506000001437) — Gneiting and Raftery | 2007-03 | 2026-07-17 | Proper scores support evaluation of probabilistic forecasts. | REPORTED | High | Scores do not establish construct or population validity. | Validation |
| E-2007 | [Uncertainty under Dataset Shift](https://proceedings.neurips.cc/paper_files/paper/2019/hash/8558cb408c1d76621371888657d2eb1d-Abstract.html) — Ovadia et al. | 2019-12 | 2026-07-17 | Accuracy and calibration can degrade under dataset shift. | REPORTED | High | Classification benchmarks, not synthetic survey panels. | Validation |
| E-2008 | [Standards for Educational and Psychological Testing](https://www.testingstandards.net/uploads/7/6/6/4/76643089/standards_2014edition.pdf) — AERA/APA/NCME | 2014 | 2026-07-17 | Validity attaches to intended score interpretation and use; reliability and fairness require evidence. | OBSERVED | High | Testing standard; application depends on SIMULA constructs. | Methodology |
| E-2009 | [Measurement Invariance Conventions](https://doi.org/10.1016/j.dr.2016.06.004) — Putnick and Bornstein | 2016-09 | 2026-07-17 | Cross-group comparisons require measurement-invariance evidence. | REPORTED | High | Review centered on structural-equation methods. | Psychographics, validation |
| E-2010 | [Model Cards for Model Reporting](https://doi.org/10.1145/3287560.3287596) — Mitchell et al. | 2019-01-29 | 2026-07-17 | Model reporting should disclose intended uses, evaluation, subgroup results, limits, and unsuitable uses. | REPORTED | High | Documentation does not itself prove safety or validity. | Model card |

## Philippine data, privacy, and governance evidence

| ID | Source | Published | Accessed | Evidence | Class | Confidence | Limitation | Used in |
|---|---|---:|---:|---|---|---|---|---|
| E-3001 | [2020 Census of Population and Housing](https://psa.gov.ph/content/2020-census-population-and-housing-cph) — PSA | 2020 reference; page n.d. | 2026-07-17 | PSA documents census coverage, sampling forms, reference date, and release schedule. | OBSERVED | High | Published aggregates or public-use files do not automatically supply every joint cohort. | Data sources |
| E-3002 | [PSA Data Archive CPH](https://psada.psa.gov.ph/catalog/CPH/about) — PSA | n.d. | 2026-07-17 | Public-use files have client-specific terms and confidentiality restrictions. | OBSERVED | High | Exact allowed commercial transformation and redistribution require term review. | Data provenance |
| E-3003 | [2023 Family Income and Expenditure Survey](https://psa.gov.ph/content/philippine-statistics-authority-conducts-2023-family-income-and-expenditure-survey) — PSA | 2023-07-10 | 2026-07-17 | PSA reports a roughly 180,000-household master sample and official income/expenditure purpose. | OBSERVED | High | Survey estimates, not psychographic or behavioral truth; access and granularity vary. | Data sources |
| E-3004 | [2022 National Demographic and Health Survey](https://psa.gov.ph/content/2022-national-demographic-and-health-survey) — PSA | 2022-11-21 | 2026-07-17 | PSA describes national demographic and health indicators and DHS collaboration. | OBSERVED | High | Sensitive domain; licensing, purpose, and variable-level fitness need review. | Data sources |
| E-3005 | [Philippine Standard Geographic Code](https://psa.gov.ph/classification/psgc/ccs) — PSA | 2026-07-13 release | 2026-07-17 | PSGC supplies versioned official geographic codes and updates. | OBSERVED | High | Boundaries and codes change; version alignment is mandatory. | Data sources, data model |
| E-3006 | [OpenSTAT API Access Guide](https://openstat.psa.gov.ph/API-Documentation) — PSA | n.d. | 2026-07-17 | API provides official statistical tables/metadata and documents a rate limit of 10 requests per 10 seconds. | OBSERVED | High | API availability is not a blanket license for all downstream uses. | Data ingestion |
| E-3007 | [DPA Implementing Rules](https://privacy.gov.ph/implementing-rules-regulations-data-privacy-act-2012/) — NPC | 2016 | 2026-07-17 | Rules define personal data, profiling, lawful processing, rights, security, and accountability including outsourced processing. | OBSERVED | High | Legal applicability depends on facts; this is not legal advice or compliance certification. | Privacy, security |
| E-3008 | [NPC Advisory 2024-04 on AI](https://privacy.gov.ph/wp-content/uploads/2025/02/Advisory-2024.12.19-Guidelines-on-Artificial-Intelligence-w-SGD.pdf) — NPC | 2024-12-19 | 2026-07-17 | NPC applies DPA duties to AI development/deployment using personal data and calls for transparency, governance, PIA, minimization, fairness, rights, and human intervention where risk warrants. | OBSERVED | High | Advisory interpretation needs use-case and counsel review. | Privacy, threat model |
| E-3009 | [NPC Advisory 2026-01 on Data Scraping](https://privacy.gov.ph/wp-content/uploads/2026/04/SGD_A_1.pdf) — NPC | 2026-04-13 | 2026-07-17 | Public availability does not remove DPA protection; scraping requires purpose, lawful basis, minimization, PIA, and respect for technical/terms boundaries. | OBSERVED | High | Applies when personal data are scraped; no scraping is authorized by Phase 0. | Data strategy, non-goals |
| E-3010 | [NPC security circular summary](https://privacy.gov.ph/npc-issues-circulars-to-strengthen-personal-data-protection-in-ph/) — NPC | 2024-04-01 | 2026-07-17 | NPC summarizes current security, PIA, access, continuity, backup, and restoration expectations under Circular 2023-06. | OBSERVED | High | Summary is not substitute for full legal/control review. | Security, operations |

## Architecture, security, and UX evidence

| ID | Source | Published | Accessed | Evidence | Class | Confidence | Limitation | Used in |
|---|---|---:|---:|---|---|---|---|---|
| E-4001 | [Next.js Authentication](https://nextjs.org/docs/app/guides/authentication) — Next.js/Vercel | n.d. | 2026-07-17 | Framework guidance places secure authorization near protected data and warns against relying only on proxy checks. | OBSERVED | High | APIs change; recheck pinned version. | Architecture, auth |
| E-4002 | [Vercel Functions](https://vercel.com/docs/functions) — Vercel | n.d. | 2026-07-17 | Function duration remains bounded; post-response work is not a durable job system. | OBSERVED | High | Plan limits change. | Deployment |
| E-4003 | [Railway Services](https://docs.railway.com/reference/services) — Railway | n.d. | 2026-07-17 | API and worker can be independently deployed services with separate commands/configuration. | OBSERVED | High | Runtime details require deployment-time verification. | Deployment |
| E-4004 | [Railway Private Networking](https://docs.railway.com/reference/private-networking) — Railway | n.d. | 2026-07-17 | Project services can communicate over runtime private DNS. | OBSERVED | High | Private networking is unavailable during builds. | Deployment, security |
| E-4005 | [Supabase RLS](https://supabase.com/docs/guides/database/postgres/row-level-security) — Supabase | n.d. | 2026-07-17 | Exposed tables need RLS; service credentials bypass RLS and must remain server-only. | OBSERVED | High | RLS does not replace privileges or API authorization. | Data model, threat model |
| E-4006 | [Securing the Supabase Data API](https://supabase.com/docs/guides/api/securing-your-api) — Supabase | n.d. | 2026-07-17 | Schema exposure/GRANT controls reachability; RLS controls rows. | OBSERVED | High | Project settings affect exposure. | Architecture |
| E-4007 | [Supabase Queues](https://supabase.com/docs/guides/queues) — Supabase | n.d. | 2026-07-17 | Postgres-native queue uses visibility timeout and explicit archive/delete semantics. | OBSERVED | High | Option was rejected because repository rules require Railway queues. | Rejected queue option |
| E-4008 | [OWASP Top 10 for LLM Applications 2025](https://genai.owasp.org/llm-top-10/) — OWASP | 2025 edition | 2026-07-17 | Prompt injection, output handling, excessive agency, disclosure, supply chain, and unbounded consumption require distinct controls. | OBSERVED | High | Guidance is not certification or exhaustive threat proof. | Threat model |
| E-4009 | [NIST AI 600-1](https://doi.org/10.6028/NIST.AI.600-1) — NIST | 2024-07-26; page updated 2026-04-08 | 2026-07-17 | GenAI lifecycle risk includes confabulation, bias, privacy, information integrity, human configuration, and value-chain risk. | OBSERVED | High | Voluntary cross-sector guidance. | Risk, evaluation |
| E-4010 | [WCAG 2.2](https://www.w3.org/TR/WCAG22/) — W3C | 2024-12-12 Recommendation | 2026-07-17 | Testable accessibility criteria cover errors, authentication, status messages, focus, input, and complete processes. | OBSERVED | High | Conformance still needs automated and manual testing. | Journeys, QA |
| E-4011 | [AAPOR Transparency Initiative](https://aapor.org/standards-and-ethics/transparency-initiative/) — AAPOR | n.d. | 2026-07-17 | Research reporting should disclose sponsor, methods, population, instruments, collection mode/language, dates, weighting, and limitations. | OBSERVED | High | Survey transparency guidance; SIMULA is not a human survey by default. | Reports, provenance |
| E-4012 | [NIST AI trustworthiness characteristics](https://airc.nist.gov/airmf-resources/airmf/3-sec-characteristics/) — NIST | n.d. | 2026-07-17 | Transparency, explainability, and interpretability are related but distinct and should be tailored to user context. | OBSERVED | High | General guidance, not a validated UI pattern. | Reports, methodology drawer |
| E-4013 | [OWASP API Security Top 10 — 2023](https://owasp.org/API-Security/editions/2023/en/0x11-t10/) — OWASP | 2023 edition | 2026-07-17 | Object/function authorization, resource limits, SSRF, configuration, inventory, and third-party API handling require explicit controls. | OBSERVED | High | Generic security baseline; SIMULA threat mapping still required. | API architecture, test strategy |
| E-4014 | [Node.js Releases](https://nodejs.org/en/about/previous-releases) and [24.18.0 release](https://nodejs.org/en/blog/release/v24.18.0) — OpenJS/Node.js | 2026-06-23 | 2026-07-17 | Node 24 is an LTS line and 24.18.0 is its current patch; Node 26 is Current. | OBSERVED | High | Status and patch versions change; recheck on dependency updates. | ADR-0002 |
| E-4015 | [Next.js 16.2](https://nextjs.org/blog/next-16-2) and npm registry `next` metadata — Vercel/npm | 2026-03-18; registry live | 2026-07-17 | Next 16.2 is stable; registry reported 16.2.10 and Node >=20.9 peer/runtime compatibility. | OBSERVED | High | Registry pins need lockfile resolution and security review. | ADR-0002 |
| E-4016 | [Python 3.14.6](https://www.python.org/downloads/release/python-3146/) and PyPI metadata — PSF/PyPI | 2026-06-10; registry live | 2026-07-17 | Python 3.14.6 is a maintenance release; selected FastAPI/Pydantic/test packages declare compatible Python ranges. | OBSERVED | High | Declared compatibility still needs install/runtime tests. | ADR-0002 |
| E-4017 | [Supabase changelog](https://supabase.com/changelog.md) — Supabase | 2026-07-10 and 2026-05-08 entries | 2026-07-17 | Supabase announced TypeScript 5+ client requirements and Node 20 support removal. | OBSERVED | High | Future package releases may tighten requirements again. | ADR-0002 |
| E-4018 | [Tables not exposed automatically](https://supabase.com/changelog/45329-breaking-change-tables-not-exposed-to-data-and-graphql-api-automatically) — Supabase | 2026-04-28 | 2026-07-17 | New public-schema tables are no longer automatically exposed to Data/GraphQL APIs. | OBSERVED | High | Project settings and migration history still require explicit verification. | ADR-0003, migrations |
| E-4019 | [Supabase changelog](https://supabase.com/changelog.md) — Supabase | 2025-10-08 | 2026-07-17 | A pgmq 1.4.4→1.5.1 rollout was halted for existing projects because an upgrade could break queue-related dependencies. | OBSERVED | High | Historical option review; Supabase Queue was rejected because repository rules require Railway queues. | Rejected queue option |
| E-4020 | [Supabase Queues](https://supabase.com/docs/guides/queues) and [Queues API](https://supabase.com/docs/guides/queues/api) — Supabase | n.d. | 2026-07-17 | Delivery is exactly once only within the visibility window; durable consumers still need idempotency across expiry/crash/archive failure. | OBSERVED | High | Option review only; SIMULA selected Railway Redis/ARQ. | Rejected queue option |
| E-4021 | [Supabase JWTs](https://supabase.com/docs/guides/auth/jwts) and [Signing Keys](https://supabase.com/docs/guides/auth/signing-keys) — Supabase | n.d. | 2026-07-17 | Asymmetric project signing keys expose JWKS for local verification; issuer, expiry, subject, role, rotation, and cache behavior require validation. | OBSERVED | High | Legacy shared-secret projects need different validation/migration treatment. | ADR-0003 |
| E-4022 | [Supabase RLS](https://supabase.com/docs/guides/database/postgres/row-level-security) — Supabase | n.d. | 2026-07-17 | UPDATE needs a SELECT policy; `WITH CHECK` constrains resulting rows; security-definer helpers should not be in exposed schemas; invoker views preserve RLS on supported Postgres. | OBSERVED | High | Functions/grants/search paths still need code-level review and tests. | ADR-0003, authorization matrix |
| E-4023 | [Railway Redis](https://docs.railway.com/databases/redis) and [private networking](https://docs.railway.com/private-networking) — Railway | pages updated 2026-05/06 | 2026-07-17 | Railway provides a Redis template/service and project-private connectivity variables; the Redis deployment is unmanaged and needs backup/monitoring. | OBSERVED | High | Provisioning, persistence, memory, HA, backup, and exact image still need environment verification. | ADR-0006, deployment |
| E-4024 | [ARQ 0.28 documentation](https://arq-docs.helpmanual.io/), [ARQ v0.28 connection source](https://github.com/python-arq/arq/blob/v0.28.0/arq/connections.py), [ARQ PyPI metadata](https://pypi.org/project/arq/), [redis 5.3.1 metadata](https://pypi.org/project/redis/5.3.1/), and [ARQ maintenance-only notice](https://github.com/python-arq/arq/issues/510) — ARQ/PyPI | live registry; notice opened 2025-10-18 | 2026-07-17 | ARQ provides asyncio Redis jobs, deferred execution, retry, unique job IDs, and pessimistic execution. ARQ 0.28 requires redis-py `>=4.2,<6`; its `create_pool()` maps `conn_timeout` only to socket-connect timeout, not in-flight command timeout. The maintainer states ARQ is maintenance-only: critical security fixes may continue, but new fixes should not be expected. | OBSERVED | High | Phase 2 requires a custom pool with separate connect/command timeout tests and exact Python 3.14/ARQ/Redis runtime proof. Phase 5 must approve a tested queue migration/exit plan before staging. | ADR-0006, R-020 |
| E-4025 | [Connect to Postgres](https://supabase.com/docs/guides/database/connecting-to-postgres), [Postgres Roles](https://supabase.com/docs/guides/database/postgres/roles), and [RLS testing guidance](https://supabase.com/docs/guides/troubleshooting/rls-performance-and-best-practices-Z5Jjwv) — Supabase | live docs | 2026-07-17 | Persistent backends may use direct or session-pooler Postgres connections; Supabase recommends separate service roles instead of sharing the `postgres` password; RLS tests can set JWT claims in the database session. | OBSERVED | High | Exact Railway IP mode, TLS chain, hosted role bootstrap, pool sizing, and transaction-local claim cleanup require implementation/environment proof. | ADR-0003, ADR-0008 |
| E-4026 | [Psycopg](https://pypi.org/project/psycopg/), [psycopg-binary](https://pypi.org/project/psycopg-binary/), [psycopg-pool](https://pypi.org/project/psycopg-pool/), and [pool documentation](https://www.psycopg.org/psycopg3/docs/advanced/pool.html) — PyPI/Psycopg | registry/docs live | 2026-07-17 | Registries reported Psycopg 3.3.4, binary 3.3.4, and pool 3.3.1 with Python 3.14 support; async pools provide bounded connection reuse and transaction commit/rollback hooks. | OBSERVED | High | Resolver, pool reset, timeout, TLS, and claim-leak behavior still require Phase 2 tests. | ADR-0002, ADR-0003 |
| E-4027 | [openapi-typescript 7.13.0 npm metadata](https://www.npmjs.com/package/openapi-typescript/v/7.13.0), [TypeScript 5.9.3 npm metadata](https://www.npmjs.com/package/typescript/v/5.9.3), and npm peer metadata for `eslint-config-next 16.2.10` — npm | registry live | 2026-07-17 | openapi-typescript 7.13.0 requires TypeScript `^5.x`; config-next's import/react/a11y plugins support ESLint through 9.x, not 10.x. Exact TypeScript 5.9.3 and ESLint 9.39.5 were selected. A clean-room npm dry-run resolved all 31 exact Phase 2 pins with exit 0 and zero `ERESOLVE` or peer-dependency warnings. | OBSERVED | High | Clean pnpm lock/install/lint/type-generation proof remains a Phase 2 gate. | ADR-0002 |
| E-4028 | [ARQ custom job serializers](https://arq-docs.helpmanual.io/#custom-job-serializers), [ARQ 0.28 job source](https://github.com/python-arq/arq/blob/v0.28.0/arq/jobs.py), and [worker failure path](https://github.com/python-arq/arq/blob/v0.28.0/arq/worker.py) — ARQ | 0.28.0 docs/source | 2026-07-17 | ARQ defaults to pickle and decodes job fields before application validation. v0.28 job envelopes use exact keys `t/f/a/k/et`; result envelopes add `s/r/st/ft/q/id`. On deserialization failure the worker supplies `f=''`, empty args, and `et=0` to result serialization; a strict serializer can fail closed to `result_data=None`. | OBSERVED | High | Safe use requires exact schemas/ranges, malformed/wrong-type/pickle and `f=''→None` tests, worker-liveness proof, exact shared configuration, and versioned rollout. | ADR-0006, T-06 |
| E-4029 | [pnpm 11 settings](https://pnpm.io/settings) — pnpm | 11.x live docs | 2026-07-17 | pnpm 11 reads project policy from `pnpm-workspace.yaml`; only authentication and registry settings are read from `.npmrc`. `autoInstallPeers`, `strictPeerDependencies`, `engineStrict`, and resolution settings are workspace configuration. | OBSERVED | High | Effective configuration and lock state still require executable checks on the pinned pnpm release. | ADR-0002, P2-01 |
| E-4030 | [Supabase local CLI](https://supabase.com/docs/guides/local-development/cli/getting-started) and [CLI reference](https://supabase.com/docs/reference/cli/getting-started) — Supabase | live docs | 2026-07-17 | The project-scoped pinned CLI supports an explicit `--workdir`, can start a Docker-backed local stack with health checks, reset its local database, and stop/delete one named local project without linking or mutating hosted state. | OBSERVED | High | Runtime proof still requires a working Docker host; CLI output includes local credentials and must be suppressed from logs. | ADR-0008, P2-01 |
| E-4031 | [Docker CLI reference](https://docs.docker.com/reference/cli/docker/) and [context inspect](https://docs.docker.com/reference/cli/docker/context/inspect/) — Docker | live docs | 2026-07-17 | `DOCKER_CONTEXT`, `DOCKER_HOST`, and client configuration can redirect daemon operations; the CLI context option overrides routing, and context inspection exposes the endpoint host. | OBSERVED | High | A local socket can still front a user-controlled daemon; the harness therefore also uses owned namespaces and refuses inherited routing. | P2-01 runtime harness |
| E-4032 | [Compose project names](https://docs.docker.com/compose/how-tos/project-name/) and [Compose application model](https://docs.docker.com/compose/intro/compose-application-model/) — Docker | live docs | 2026-07-17 | The CLI project-name flag has highest precedence and Compose labels resources with the project name, enabling per-run isolation and exact cleanup. | OBSERVED | High | Port bindings remain host-global; the harness separately refuses occupied fixed test ports and concurrent runs. | P2-01 runtime harness |
| E-4033 | [Python 3.14 `http.client`](https://docs.python.org/3.14/library/http.client.html) — PSF | 2026-07 live docs | 2026-07-17 | `HTTPConnection` can address the fixed loopback host directly and exposes redirect status without automatically following it. | OBSERVED | High | A successful health response proves only that the local endpoint answered, not the correctness of all Supabase services. | P2-01 runtime harness |

## Phase 2 local implementation evidence

| ID | Source | Observed | Evidence | Class | Confidence | Limitation | Used in |
|---|---|---:|---|---|---|---|---|
| E-5001 | Exact local toolchain plus `package.json`, `pnpm-lock.yaml`, `pyproject.toml`, and `uv.lock` | 2026-07-17 | Node 24.18.0, pnpm 11.13.1, Python 3.14.6, uv 0.11.19, and Supabase CLI 2.109.1 pass exact checks; both frozen dependency graphs are current; ARQ/redis-py/hiredis import on the exact interpreter. | OBSERVED | High | User-local Windows evidence plus locks; Linux CI has not run because repository is uncommitted/unpublished. | P2-01, ADR-0002 |
| E-5002 | Local quality commands and generated artifacts | 2026-07-17 | Format, lint, strict type checks over 30 Python source files, 4 JS/TS contract/component tests, 61 Python unit tests with 2 POSIX-only lock tests expected-skipped on Windows, Next production build, byte-level contract regeneration, 148-file secret baseline, Compose parse, effective pnpm-policy assertion, and npm/Python audits pass with no known Moderate-or-higher npm or known Python vulnerability. | OBSERVED | High | Static/host evidence only; runtime evidence is E-5008. Regex secret scanning is baseline only; full M6 scanning remains required. | P2-01, P2-07 |
| E-5003 | Docker Desktop backend log, WSL status, and `Win32_Processor` on the implementation host | 2026-07-17 | Docker's WSL import fails with `HCS_E_HYPERV_NOT_INSTALLED`; engine API remains unavailable; the processor reports `VirtualizationFirmwareEnabled=False`; WSL 2 is installed with no distribution. | OBSERVED | High | Host-specific. Windows optional-feature state was not observable without elevation. Requires user/administrator remediation and normally reboot; says nothing about Linux CI health. | R-021, P2-01 blocker |
| E-5004 | Docker Hub/GHCR manifest APIs and `docker buildx imagetools inspect` | 2026-07-17 | Committed multi-platform index digests match Redis 8.2.7 Alpine, Node 24.18.0 bookworm-slim, Python 3.14.6 slim-bookworm, and uv 0.11.19. | OBSERVED | High | Manifest identity is not a successful local build, runtime UID proof, SBOM, or container vulnerability scan. | P2-01 containers/CI |
| E-5005 | Independent P2-01 static confirmation review | 2026-07-17 | After two correction rounds, the reviewer reported PASS with 0 Critical, 0 High, and 0 Medium; 31/31 Python unit tests and effective pnpm/Supabase CLI controls were independently rechecked. | OBSERVED | High | Static/repository acceptance only; later runtime proof is E-5008. | P2-01 static gate |
| E-5006 | `scripts/verify_m0_runtime.py`, twenty-six platform-aware unit tests, adversarial local preflight, and initial exact full-gate execution | 2026-07-17 | The harness strips inherited Docker/Compose/Supabase routing, accepts and re-injects only a validated local socket/pipe context, refuses linked/nonlocal/occupied/pre-existing/cross-clone-concurrent state, uses a private per-user lock and per-run service/image/probe ownership, discards both credential-bearing output streams, and contains command trees with kill-on-close Windows Job Objects or POSIX PGID escalation. It rejects proxy/redirect health proof, preserves primary failures, and attempts exact cleanup after failure, timeout, or catchable interruption. On Windows, 24 applicable tests pass and 2 POSIX-only lock tests skip; coverage includes forced owner death, exited/nonzero leaders, stubborn descendants, and probe timeout cleanup. With hostile routing variables injected, real preflight selected `desktop-linux` at the local named-pipe endpoint and failed at unavailable `docker info`, leaving zero runtime resources. | OBSERVED | High | Initial blocked-host/fail-closed evidence; later successful runtime proof and Compose-boundary extension are E-5008. Independent safety PASS is E-5007. | P2-01 runtime gate, R-021 |
| E-5007 | Independent final M0 runtime-harness safety review | 2026-07-17 | After correction rounds covering routing, ownership, credential output, health probing, bounded execution, process-tree containment, forced owner death, exact resource cleanup, and private lock safety, the reviewer reported PASS with 0 Critical, 0 High, and 0 Medium. It independently ran 24 applicable harness tests with 2 expected POSIX-only skips on Windows; Ruff and mypy were clean. | OBSERVED | High | Safety/orchestration review only; later successful runtime proof is E-5008. | P2-01 runtime harness review, R-021 |
| E-5008 | Exact `pnpm verify:m0-runtime`, current-tree quality/SCA gates, and post-run Docker/temp audit | 2026-07-17 | After user host remediation, Docker 29.6.1 on validated `desktop-linux` passed the full local-only gate: canonical Compose required Redis TCP 6379 on `127.0.0.1` and one non-internal Compose DNS network; Redis 8.2.7 ran non-root; 3/3 exact ARQ startup/retry/hard-crash/shutdown integrations passed; Supabase Auth health and database reset passed; pinned web/API/worker images built and ran as `node`/`simula`/`simula`; API import and worker lifecycle probes passed. The 32-test harness suite has 30 applicable Windows passes and 2 expected POSIX-only skips, including five fail-closed Compose-boundary cases. Cleanup left zero run-owned containers, networks, images, temp directories, or hosted link markers. Current format/lint/type/unit/build/contract/secret/Compose/SCA checks pass. | OBSERVED | High | Local Windows evidence only; Linux CI remains unexecuted because the repository has no commit/remote. This proves P2-01 mechanics, not tenant schema, product behavior, scientific validity, or production readiness. | P2-01 exit, R-021 mitigation |
| E-5009 | First ordered migration, `pnpm verify:m1-database`, full repository gate, and combined local integration/SCA run | 2026-07-17 | P2-02 passes two consecutive zero-state Supabase resets; database lint; 32/32 catalog-derived pgTAP assertions over four least-privilege roles, 14 forced-RLS tables, 16 policies, exact grants/functions, tenant FKs, and authored fixtures; three local Auth sign-ins; anonymous plus three authenticated Data API denial matrices; real `session_user=simula_api` claim allowlist/expiry/transaction-reset tests; direct-DML and owner-role denial; two-tenant/self-membership isolation; atomic organization/idempotency/audit effects; replay/conflict; injected post-write failure rollback; and a privileged local organization/membership/idempotency/audit deletion-graph skeleton. Generated `api`/`private` TypeScript database types byte-match pinned CLI output. Combined Redis/database integration passes 5/5; `pnpm check`, 157-file secret scan, and Moderate-blocking npm/Python SCA pass. | OBSERVED | High | Disposable local Windows/Supabase evidence only; Linux CI and independent M1 review have not run. This proves P2-02 database/Auth boundaries, not FastAPI JWT validation, P2-03 domain APIs/UI, full content/run deletion, backup tombstones, worker behavior, scientific validity, or production readiness. | P2-02 exit, AC-ORG-001/002 partial, SEC-RLS-001, SEC-DATA-API-001, SEC-ROLE-001, SEC-CLAIMS-001, PRIV-DEL-001 skeleton, R-013, R-018 |
| E-5010 | P2-03 local API/UI runtime and final review gate | 2026-07-18 | Exact Node 24.18.0/pnpm 11.13.1 and Docker 29.6.1 pass the reset-driven gate: two clean Supabase resets, lint, 32 pgTAP checks, 26 API tests, five database/Auth/API/Redis integrations, database/OpenAPI drift checks, format/lint/strict types, five web tests, Next 16.2.10 build, secret scan, and Moderate-blocking npm/Python SCA. The final correction loop proves atomic Redis buckets; pre-auth IP protection/refund; 24-hour idempotency replay bypass; DB-15 exact-key cleanup; JSON-only content policy; forged/expired/rotation JWT proof; audit outcome/source/denied records; and browser-readable correlated CORS errors. Two independent final reviewers reported no remaining actionable finding. Existing Playwright proof covers Auth sign-in through a second immutable version with a distinct SHA-256. | OBSERVED | High | Disposable local Windows evidence only. Hosted Supabase migration has not run: CLI authentication is absent and remote migration history/state remain unverified. Linux CI and Phase 2 M3–M6 remain outstanding. | P2-03 exit, AC-AUTH-001/002 partial, AC-ORG-001/002 partial, AC-PROJ-001/002 partial, AC-ERR-001 partial, E2E-ORG-001, E2E-PROJ-001, SEC-LIMIT-001, LOAD-RATE-001 |
| E-5011 | Supabase project `ywiwmczccktwzqyhzhiz` hosted P2-03 schema apply | 2026-07-18 | MCP-authorized project access as `kurtgav` confirmed the active Simula project. The idempotent no-password role bootstrap created `simula_api`, `simula_worker`, and both NOLOGIN owner roles with no unsafe attributes. The three checked-in P2-03 migrations were applied without `seed.sql`; remote history was reconciled to versions `20260717000100`, `20260717025314`, and `20260718000000`. Catalog verification reports 9 empty `api` and 5 empty `private` RLS tables. | OBSERVED | High | This is hosted schema/role state only: no runtime passwords, application deployment, fixture data, production data, or M3 migration was applied. A pre-existing `public.rls_auto_enable()` security-advisor warning remains outside SIMULA source; private outbox/attempt/event RLS notices are expected until M3 worker policies exist. | ADR-0008, P2-03 hosted schema evidence |
| E-5012 | Supabase project `ywiwmczccktwzqyhzhiz` hosted P2-04 durable-run migration | 2026-07-18 | After a clean local reset plus 41 pgTAP catalog/ACL assertions, MCP-authorized access as `kurtgav` applied checked-in migration `20260718010000_phase2_runs_and_worker`. History was reconciled to the Git timestamp. Remote verification found exactly one fixed global authored-demo audience/version, zero tenant/project/stimulus/run/result/outbox data, seven private worker-owner policies, no direct `simula_worker` run-table DML, worker execution access to `private.claim_run_execution`, and no equivalent API grant. The former private outbox/attempt/event no-policy notices cleared. | OBSERVED | High | Schema authority only: API/dispatcher/worker runtime credentials, queue transport, execution integration, and application deployment remain unimplemented. The pre-existing `public.rls_auto_enable()` anon/authenticated warning remains outside SIMULA source. | P2-04 durable DB slice, ADR-0003/0004/0006/0008 |
| E-5013 | P2-04 end-to-end run proof and hosted runtime-lock corrections | 2026-07-18 | A clean local Supabase reset, database lint, pgTAP suite, Redis health check, and integration test prove one API-created run survives 20 sequential idempotency replays, durable outbox dispatch, canonical Redis job proof, role-pinned worker completion, duplicate delivery no-op, result publication, and a retry failure transition. Hosted project `ywiwmczccktwzqyhzhiz` has forward migrations `20260718020000`–`20260718020400` applied with history matching Git. Remote catalog verification confirms fixture/result/organization write locks were removed where they were redundant, both owner roles have only `extensions` schema usage, and the command/worker owners still lack organization/result UPDATE authority. | OBSERVED | High | This proves the disposable local full path and hosted schema authority, not a hosted application deployment, runtime credentials, real provider behavior, production load, or scientific validity. Security advisor still reports the pre-existing unrelated public `rls_auto_enable()` function; performance advisor reports pre-scale index suggestions. | P2-04 exit, ADR-0003/0004/0006/0008, E2E-RUN-001 |

| E-5014 | P2-05 trustworthy accessible result-experience gate | 2026-07-18 | A disposable local browser gate passes terminal result/provenance, safe inaccessible-run error, bounded queued-to-terminal polling, keyboard disclosure, desktop/mobile Axe, and 390px responsive proof. The result contract preserves signed 64-bit seeds as canonical strings and models unavailable output explicitly; UI telemetry is content-free; legacy provenance is never reconstructed; the server can hide result presentation without deleting API data. Full `pnpm check` passed formatting, lint, strict types, 35 web tests, 141 Python tests, 2 expected Windows platform skips, web build, generated contracts, claim policy, and secret scan. | OBSERVED | High | Local disposable evidence only. It does not prove hosted application deployment, cancellation/retry behavior, load targets, complete Phase 2 traceability, human comprehension, or scientific validity. | P2-05 exit, AC-RES-001/002/003, AC-ERR-001, AC-A11Y-001/002, E2E-RESULT-001, E2E-ERROR-001, E2E-POLL-001, A11Y-AXE-001, SEC-XSS-001 |
| E-5015 | P2-06 cancellation command and cancel-wins race gate | 2026-07-18 | Forward migration `20260718040000_phase2_cancellation` resets cleanly and the 41-assertion pgTAP suite passes. It grants only owner/editor cancellation through a narrow command function, records cancel request/audit events, finalizes no-lease cancellation before outbox dispatch, excludes canceled intent from dispatch claims, and turns completion/failure races into terminal `canceled` with no result. API unit tests prove `202 cancel_requested` and terminal-race `200`; a real local API/Redis/worker integration proves viewer denial, queued finalization, terminal idempotency, and cancel-wins completion; generated database/OpenAPI artifacts match. `pnpm verify:p2:e2e` passes four browser proofs including the cancel command and no-substitute-result state; full `pnpm check` passes 143 Python tests, 2 expected platform skips, 35 web tests, build, contract drift, claim policy, and secret scan. | OBSERVED | High | Local disposable application evidence only; retry timing/exhaustion, poison handling, lease recovery, failure UI, P2-07 traceability/CI, and Phase 2 exit review remain open. Hosted schema proof is E-5016. | P2-06 partial, AC-RUN-004 partial, AC-ERR-001, API-CANCEL-001, INT-CANCEL-RACE-001, E2E-CANCEL-001 |
| E-5016 | Hosted P2-06 cancellation and ACL-cleanup schema apply | 2026-07-18 | MCP-authorized access applied checked-in migrations `20260718040000_phase2_cancellation` and `20260718041000_revoke_command_api_create` to project `ywiwmczccktwzqyhzhiz` without seed or application data. Migration history was reconciled and `supabase migration list --linked` reports exact local/remote equality through `20260718041000`. Remote catalog checks confirm the API cancel wrapper, private cancel command and finalizer exist; `simula_api` and `simula_worker` have the intended execute grants; `simula_command_owner` lacks CREATE on both `api` and `private`. | OBSERVED | High | Hosted schema/ACL authority only: no hosted API/worker deployment, runtime credentials, customer data, provider call, retry/recovery proof, or production readiness claim. Advisor warnings remain: pre-existing `public.rls_auto_enable()` exposure and pre-scale index/policy suggestions. | P2-06 hosted schema, ADR-0003, ADR-0008 |
| E-5017 | P2-06 timeout retry/backoff/exhaustion gate and hosted schema apply | 2026-07-18 | Forward migration `20260718050000_phase2_retry_backoff` returns an atomic worker-only retry resolution: 5 seconds after attempt 1, 30 seconds after attempt 2, then terminal failure at the authoritative three-attempt cap. The worker turns only a provider timeout into `Retry` after that database CAS; other provider errors remain terminal. Fresh reset passes 41 pgTAP assertions including command/worker-owner schema-CREATE denial. Real local API/outbox/worker integration proves 5s then 30s defers, `retrying` state, third-attempt failure, no result, and three provider calls. Full quality gate passes 144 Python tests, 2 expected platform skips, 35 web tests, build, type/contract drift, claim policy, and secret scan. Hosted migration history exactly matches Git through `20260718050000`; the failure helper, delay token, worker execute grant, API denial, and worker-owner CREATE revocation were catalog-verified. | OBSERVED | High | Timeout is the only retryable provider class in this increment. Poison handling, stale lease supersession, Redis-loss reconciliation, failure UI, P2-07 traceability/CI, and Phase 2 exit review remain open. Hosted proof remains schema/ACL only, not an application deployment or production readiness claim. | P2-06 partial, AC-RUN-004 partial, INT-RETRY-001, ADR-0006 |
| E-5018 | P2-06 stale-dispatch recovery and hosted schema apply | 2026-07-18 | Forward migration `20260718060000_phase2_stale_dispatch_recovery` adds a worker-only `private.reconcile_run_dispatch(integer,boolean)` helper. Under run-then-outbox locks it selects stale/force-recovered queued, running, or retrying work with no active worker lease; it marks the prior outbox terminal, increments a bounded dispatch generation, creates one canonical replacement, records event/audit evidence, and terminalizes recovery exhaustion at generation three. The dispatcher invokes recovery before due dispatches and on a bounded cadence. A fresh local reset passes 41 pgTAP assertions; the real local Redis/worker integration expires an active lease, proves generation-one supersession, and confirms/claims generation two exactly once. Generated database contracts and every constituent repository gate pass: format, lint, strict type checks, 144 Python tests with 2 expected Windows skips, 35 web tests, build, contract drift, claims policy, and secret scan. Hosted migration history exactly matches Git through `20260718060000`; catalog verification confirms the function, worker-only execute, API denial, worker-owner CREATE revocation, narrow recovery INSERT policy/grant, and both replacement/exhaustion guards. | OBSERVED | High | This proves local recovery behavior and hosted schema/ACL authority only. No hosted application/worker runtime, runtime credential, seed/customer data, provider behavior, user-facing failure E2E, poison-race proof, or production readiness claim is made. | P2-06 partial, AC-RUN-004 partial, INT-OUTBOX-001, R-017, R-019, ADR-0006 |
| E-5019 | P2-06 poison-dispatch terminalization and hosted schema apply | 2026-07-18 | Forward migration `20260718070000_phase2_poison_dispatch` adds worker-only `private.finalize_poisoned_dispatches(integer)`. Under run-then-outbox locks it terminalizes only expired, unconfirmed current-generation claims at the immutable tenth dispatch attempt, transitions a non-leased queued/retrying run to `failed`, and records `dispatch_exhausted` run-event/audit evidence. The dispatcher finalizes cancellation then poison before recovery/claims; a definite non-ambiguous transport error is recorded through the existing dispatch-failure CAS, while ambiguous or unproven publish states remain lease-bound to avoid false terminalization. Fresh reset, lint, generated type drift, and all 41 pgTAP assertions pass. Worker unit tests prove transport-failure recording; five real local API/Redis/worker integrations prove terminal poison exhaustion and cancellation winning the same stale tenth-claim race. Every constituent repository quality gate passes: format, lint, strict types, 145 Python tests with 2 expected Windows skips, 35 web tests, build, contract drift, claims policy, and secret scan. Hosted history exactly matches Git through `20260718070000`; catalog verification confirms the function, worker-only execute, browser/API denial, worker-owner CREATE revocation, ten-attempt guard, terminal token, and `SKIP LOCKED` guard. | OBSERVED | High | Local runtime and hosted schema/ACL only; no hosted API/worker runtime, runtime credential, seed/customer data, provider behavior, additional retry-class proof, user-facing failure E2E, or production readiness claim. Supabase advisors retain the unrelated public `rls_auto_enable()` warning and pre-scale index/policy notices. | P2-06 partial, AC-RUN-004 partial, INT-OUTBOX-001, INT-CANCEL-RACE-001, R-017, R-019, ADR-0006 |
| E-5020 | P2-06 failed-run browser experience gate | 2026-07-18 | The failed terminal state now explicitly says that SIMULA will not substitute a result. `E2E-FAIL-001` runs in the disposable local API/worker/web browser runtime, supplies a typed failed run, verifies the safe no-substitute-result copy, confirms no result renderer appears, observes no result request, and proves the bounded poller stops after one terminal status read. The full five-test browser gate also passes result/accessibility, inaccessible-run error, polling, and cancellation flows. Repository format/lint/type checks, 145 Python tests with 2 expected Windows skips, 36 web tests, production build, generated contract drift, claim policy, and secret scan pass. | OBSERVED | High | Browser/local only; it does not add a failure reason to the public run contract, additional provider retry classes, hosted runtime proof, seed/customer data, or production-readiness claim. | P2-06 partial, AC-RUN-005/006 partial, E2E-FAIL-001, AC-ERR-001 |
| E-5021 | P2-06 bounded safe provider retry classes | 2026-07-18 | The worker now treats exactly three provider failure classes as retryable: timeout, explicit preflight-unavailable, and explicit rate-limit rejection. Each must still receive a database CAS resolution; that function alone selects 5 seconds, then 30 seconds, then terminal failure. Unknown/ambiguous provider exceptions remain the existing terminal `execution_provider_failure` path. Worker unit tests assert the safe codes and reject no extra completion; the real local API/Redis/worker integration runs all three classes through retrying, 5s/30s defers, three calls, terminal failure, and no result. Full P2 pipeline passes 7 integrations; repository format/lint/type checks, 147 Python tests with 2 expected Windows skips, 36 web tests, production build, generated contract drift, claim policy, and secret scan pass. | OBSERVED | High | This adds no provider adapter, outbound call, hosted schema/runtime mutation, seed/customer data, or production-readiness claim. Retry authority remains fixed to explicit pre-submit/reject classes and the database budget. | P2-06 complete, AC-RUN-005/006, INT-RETRY-001, ADR-0006 |
| E-5022 | P2-07 implementation gate and exit-audit snapshot | 2026-07-18 | Clean local Supabase reset/lint/41 pgTAP assertions, ten real API/Redis/worker integrations, and five browser flows pass. `pnpm check` passes formatting, lint, generated artifacts, policy/security scans, 151 Python tests with 2 expected platform skips, and 36 web tests; explicit Linux mypy passes 65 sources. The hosted project history equals local migrations through `20260718094407`, including the global backpressure and hardened-function-ACL migrations. GitHub Actions `29640798631` for `a521926` passes its disposable Supabase/browser/Linux quality-security and non-root image/import gates. | OBSERVED | High | Local/disposable runtime, hosted schema/ACL, and CI evidence only. It does not prove a hosted API/worker deployment, runtime credentials, seed/customer data, provider behavior, production load, scientific validity, or the required independent Phase 2 exit review. | P2-07 implementation gate, INT-BACKPRESSURE-001, INT-WORKER-LIMIT-001, SEC-LIMIT-001, LOAD-RATE-001, E2E-POLL-001, ADR-0002–0010 |
| E-5023 | Ten-assignment independent Phase 2 exit review | 2026-07-18 | Ten bounded reviewer assignments inspected product acceptance, database security, queue/worker behavior, API/auth contracts, browser accessibility, CI/supply chain, observability/operations, method/data integrity, documentation integrity, and the meta exit gate. Result: FAIL with no reported Critical finding and unresolved High findings. The High themes are missing failed-run correlation and pre-run audience disclosure/API; expired lease mutation authority; ARQ try-budget and database timeout drift; replay admission ordering, cancel rate scope, and dependency deadlines; Auth/keyboard/screen-reader evidence; SBOM/container scanning and enforceable branch checks; traces/metrics/log redaction/readiness/audit/operational controls; fail-closed result schema and explicit no-egress proof; deterministic 100-repeat/cross-process, concurrent replay, 30-run p95, deletion, and evidence-integrity gaps. Medium findings include stale-attempt closure, heartbeat use, forged-dispatch telemetry, OpenAPI compatibility, transient SQLSTATE mapping, idempotency reuse, Windows CI, secret-history scanning, backup drills, and stale governance notes. | OBSERVED | High | Review assignments are code/evidence audits performed by independent agent contexts, not an external human certification. Overlapping findings are grouped, not represented as a deduplicated count. Phase 3 remains blocked until all Critical/High findings are cleared and the exit review passes. | Phase 2 exit FAIL, P2-07 remediation backlog |
| E-5024 | Expired-lease authority and stale-attempt remediation | 2026-07-18 | Forward migration `20260718113445_20260718111531_phase2_lease_attempt_hardening` makes expired run/attempt leases non-renewable and non-mutation-capable, closes stale attempts as `superseded`, and closes cancellation-finalized attempts as `canceled`. Red integration proof first observed an expired heartbeat returning true and canceled attempts remaining `running`; after the migration, the three focused recovery/cancellation/poison cases pass, and the complete run pipeline passes 10/10. A clean local replay, database lint, 41/41 pgTAP assertions, generated database-type drift, local security advisor, and full `pnpm check` pass; the latter includes 151 Python passes, 2 expected platform skips, 36 web tests, strict types, production build, generated contracts, forbidden-claim policy, and 298-file secret baseline. The seed-free migration is applied to hosted project `ywiwmczccktwzqyhzhiz`; remote history contains version `20260718113445`, all four function-definition hashes exactly match local and retain `simula_worker_owner`, `SECURITY DEFINER`, empty search path, and RLS-on configuration, and the hosted security advisor reports zero lints. Hosted data remains one immutable authored-demo audience/version and no tenant/run rows. Commit `941f825` is pushed; GitHub Actions run `29643209461` passes its disposable Supabase/browser/Linux quality-security and non-root image/import gates. | OBSERVED | High | Closes only the expired-lease/stale-attempt review item. Remaining E-5023 High/Medium findings are still required. No hosted API/worker runtime, credential, customer data, seed, provider call, or production-readiness claim is included. | P2-07 remediation, R-017, R-019, DB/worker authority |
| E-5025 | Fail-closed Phase 2 result-write contract | 2026-07-18 | Red integration proof first showed that the worker-authority database function accepted extra top-level and nested result fields plus mismatched frozen provenance. Forward migration `20260718122048_20260718120823_phase2_result_contract_boundary` adds an exact private validator and makes `private.complete_run_execution` reject unknown fields, malformed nested values, noncanonical demo/unavailable output variants, and run/manifest/seed provenance drift before immutable persistence. A clean local replay, database lint, 41/41 pgTAP assertions, generated database types, 11/11 complete run integrations, focused red/green contract proof, strict Python checks, contract gates, and secret scan pass. The seed-free migration is applied to hosted project `ywiwmczccktwzqyhzhiz`; remote/local definitions have matching MD5 values `b71087bd6454770ec1f5cb4b5bd09ac9` and `f119d1ff9a588106ad243596c4d09b6c`, retain least-privilege owner/configuration/ACL, and the hosted security advisor reports zero lints. Hosted data remains one immutable authored-demo audience/version and no tenant/run rows. Commit `1ee0f5a` is pushed; GitHub Actions run `29644323387` passes disposable Supabase, five-browser, Linux quality/security, and non-root image/import gates. | OBSERVED | High | Closes the arbitrary-result-persistence half of R-026 only; E-5027 subsequently closes SEC-EGRESS-001. Other E-5023 findings remain open. No hosted API/worker runtime, credential, customer data, seed, provider call, or production-readiness claim is included. | P2-07 remediation, R-026, SCHEMA-RESULT-001 |
| E-5026 | Worker retry, deadline, heartbeat, and rejection-telemetry alignment | 2026-07-18 | Red tests exposed the missing shared ARQ try constant, absent worker transaction-local SQL deadlines, unused execution heartbeat, and silent malformed/stale binding rejection. Commits `0ca3eca` and `7a66361` make the codec and worker share transport `max_tries=16`, set `statement_timeout=8s`, `lock_timeout=2s`, and `idle_in_transaction_session_timeout=10s` on every worker command transaction, require a current lease heartbeat before provider work, and emit only allowlisted binding/claim rejection reasons without payload or raw status. Focused worker/core suites pass 85/85, strict Ruff and mypy pass 66 sources, a clean-reset real worker/Postgres result flow passes, and secret scans pass. GitHub Actions runs `29644976771` and `29645325104` pass disposable Supabase, five-browser, Linux quality/security, complete integration, and non-root image/import gates. | OBSERVED | High | Mitigates R-027 for the Phase 2 deterministic provider. It does not authorize a real/long-running provider; future adapters need cancellable periodic heartbeats and provider-specific timeout proof. No hosted schema/runtime, credential, seed/customer data, provider call, or production-readiness claim is included. | P2-07 remediation, R-027, INT-WORKER-LIMIT-001, ADR-0006 |
| E-5027 | Fail-closed deterministic worker no-egress proof | 2026-07-18 | Commit `b732a5b` adds a fixed `--verify-no-egress` worker probe that refuses any interface set except loopback, runs the deterministic provider through its production contract, and emits only an allowlisted status/provider/interface report. The mandatory Linux container gate runs the probe with Docker networking disabled, a read-only root filesystem, all capabilities dropped, and `no-new-privileges`; the image remains the existing non-root `simula` runtime. Focused worker/core tests pass 87/87, strict Ruff and mypy pass 66 sources, workflow formatting, secret scanning, and npm/Python SCA pass, and the exact hardened container command passes locally. GitHub Actions run `29645866096` passes disposable Supabase, five-browser, Linux quality/security, complete integration, non-root image/import, and the named no-egress gate. | OBSERVED | High | Together with E-5025, mitigates R-026 for the Phase 2 deterministic provider. Any real provider, extra container network, provider credential, or hosted worker runtime remains prohibited and requires a new threat review and egress policy. No hosted schema/runtime, credential, seed/customer data, external provider call, or production-readiness claim is included. | P2-07 remediation, R-026, SEC-EGRESS-001, ADR-0007 |
| E-5028 | Container SBOM and fixable-vulnerability enforcement | 2026-07-18 | Commit `d85a670` installs checksum-pinned Syft 1.48.0 and Grype 0.116.0 directly from immutable release assets, generates CycloneDX SBOM plus full Grype JSON for the web/API/worker images, uploads the six files through full-SHA-pinned `actions/upload-artifact`, and fails the named container gate on any fixable High/Critical finding. The first execution, GitHub Actions `29646538339`, correctly failed on bundled runtime-only npm/Undici 6.26.0. Commit `bffe83b` removes npm/Corepack binaries and modules from the web runtime while retaining Node 24.18.0; an exact clean-commit image locally had no fixable High/Critical findings. GitHub Actions `29646850994` then passed the full disposable database/browser/Linux quality-security and three-image container gate in 8m38s and uploaded artifact `container-security-bffe83b698a1ac691e6c4abbeb0af90bed9d3b51` (artifact ID `8430363527`). The exact Python HTMLParser advisory in `.grype.yaml` is the sole reviewed exception: SIMULA does not import/invoke the affected parser, no stable Python 3.14 fix exists, and review is due by 2026-08-01 or before hosted use. | OBSERVED | High | Closes the SBOM/container-scan portion of R-025, not the risk. History-aware secrets, Windows CI, one root verification command, and enforceable required checks remain open. Full reports intentionally preserve unfixed findings for review; the admission blocker covers fixable High/Critical findings and the exact dated exception only. No hosted schema/runtime, credential, seed/customer data, provider call, or production-readiness claim is included. | P2-07 remediation, R-025, supply-chain gate |
| E-5029 | History-aware secret scan and exact Windows quality gate | 2026-07-18 | Commit `9f7b12f` adds checksum-pinned Gitleaks 8.30.1 against every reachable commit and a Windows Server 2025 gate using exact Node 24.18.0, pnpm 11.13.1, Python 3.14.6, and uv 0.11.19. `.gitleaks.toml` narrows its only exception to generic-key matches on test-only idempotency/create-key assignments in two exact test files; local review of the then-current 51 commits and GitHub Actions `29647492906` over 52 commits both found no leak. The same CI run passed all four named jobs: the Windows gate completed frozen installs, exact-toolchain verification, format/lint/types over 66 Python sources, 36 web tests, 160 Python passes with two expected POSIX-only skips, production build, generated/policy/secret gates, and npm/Python SCA; the disposable Linux foundation and three-image no-egress/SBOM/vulnerability gates also passed. Live GitHub API checks for main branch protection and repository rulesets both returned `403` with the private-plan upgrade/public-repository requirement. | OBSERVED | High | Closes the history-secret and Windows portions of R-025, not the risk. One root verification command remains in remediation. Required-check enforcement remains blocked until an authorized plan/governance change; repository visibility and billing were not changed. No hosted schema/runtime, credential, seed/customer data, provider call, or production-readiness claim is included. | P2-07 remediation, R-025, CI portability and history gate |
| E-5030 | Consolidated root Phase 2 verification gate | 2026-07-18 | Commit `6524011` adds root `pnpm verify` and makes the named Linux foundation job invoke it after disposable Redis/Supabase startup and Playwright installation. The command orders the reset-driven M2 API/database contract, five-browser P2 E2E, repository format/lint/types/unit/build/generated/policy/secret gates, complete 19-case integration set, and npm/Python SCA. GitHub Actions `29648136756` passed all four jobs in about eight minutes: the root foundation gate completed 41 pgTAP assertions, five browser proofs, strict types over 66 Python sources, 36 web tests, 160 Python passes with two expected cross-platform skips, production build/generated/policy/secret checks, 19 integrations, and zero known npm/Python vulnerabilities; history, exact Windows, hardened no-egress, and three-image SBOM/vulnerability jobs also passed. | OBSERVED | High | Closes the root-command portion of R-025, not the risk. Enforceable required checks remain blocked by the current private GitHub plan's branch-protection/ruleset `403`; repository visibility and billing were not changed. The root command assumes its documented disposable Supabase dependency is already running and leaves lifecycle cleanup to CI. No hosted schema/runtime, credential, seed/customer data, provider call, or production-readiness claim is included. | P2-07 remediation, R-025, root verification gate |
| E-5031 | Final Phase 2 implementation, observability, hosted-schema, and audit gate | 2026-07-18 | Root `pnpm verify` exits 0 after two clean database resets, clean lint, 58/58 pgTAP assertions, 60/60 API tests, 9/9 browser flows, 198 non-integration Python passes with 2 expected Windows skips, 43/43 web tests, 2/2 contract tests, 22/22 complete integrations, production builds, generated DB/OpenAPI drift, forbidden-claim and secret gates, and zero known npm/Python vulnerabilities. The increment puts general rate admission before sign-in audit work and adds bounded live API/worker measurements for database query duration/count, pool use, migration/RLS status, durable state counts, cancellation age, stuck leases, visibility extension, duplicate delivery, invalid transitions, retry/terminal failure, provider failure class, and cancellation finalization. A fixed aggregate snapshot exposes no row/content/tenant labels and is executable only by `simula_api`/`simula_worker`. Checked-in migration `20260719040000` was the sole linked dry-run delta, applied seed-free to `ywiwmczccktwzqyhzhiz`; local/remote histories match and linked lint is clean across all schemas. The final vault audit passes 58/58 frontmatter, 151/151 wikilinks, and 104 unique evidence definitions with zero undefined references. Prior pushed head `95d71ed` has green GitHub Actions run `29661559207`. | OBSERVED | High | This is a local/disposable implementation gate plus hosted schema/ACL evidence. It is not a hosted API/worker/web deployment, customer-data load, real-provider call, scientific validation, human screen-reader pass, independent exit re-review, enforceable required-check policy, or production-readiness claim. Five Medium findings remain in the final audit. | P2-07 remediation, ADR-0009, R-023–R-025, Phase 2 audit |
| E-5032 | Phase 2 audience, rate-marker, and contract-governance remediation | 2026-07-19 | Root `pnpm verify` exits 0 after two clean database resets, clean lint, 59/59 pgTAP assertions, 61/61 API tests, 9/9 browser flows, 204 non-integration Python passes with 2 expected Windows skips, 43/43 web tests, 2/2 contract tests, and 22/22 complete integrations plus production builds, generated DB/OpenAPI drift, policy, secret, and SCA gates. Migration `20260719050000_phase2_audience_v2_immutability.sql` retains revoked v1, activates immutable semantic v2 with the exact canonical manifest checksum, enforces one active version, and selects runtime data through the stable audience identity. Redis markers now bind actor, organization, resource, command route, and idempotency key with exact attempt ownership, fixed TTLs, best-effort accepted promotion, and atomic run user/organization buckets; the focused final independent review reports PASS with no High or Medium finding. A typed RFC 9457 inventory including `request_deadline_exceeded` is generated into OpenAPI, and the base-aware compatibility classifier fails closed in Linux and Windows CI. The seed-free migration is applied to hosted project `ywiwmczccktwzqyhzhiz`; local/remote histories match through `20260719050000`, linked lint and security advisors are clean, and remote catalog checks confirm the version/checksum/ACL/runtime-selection invariants. The final vault audit passes 58/58 frontmatter, 151/151 wikilinks, 105 unique evidence definitions with zero duplicates or undefined references, and active-plan sections 1–11. | OBSERVED | High | This is local/disposable application proof plus hosted schema/ACL evidence. It is not a hosted application deployment, customer-data load, real-provider call, scientific validation, human screen-reader pass, complete independent exit re-review, enforceable required-check policy, user-facing deletion orchestration, full application-compatible staging restore, or production-readiness claim. Existing performance-advisor recommendations remain monitored under R-031. | P2-07 remediation, R-028–R-031, ADR-0002, ADR-0005, Phase 2 audit |
| E-5033 | Final Phase 2 code-remediation, operator-control, hosted-schema, and independent re-review gate | 2026-07-20 | Root `pnpm verify` exits 0 after clean database replay/lint with 68/68 pgTAP assertions, 64/64 API tests, 11/11 Playwright flows, 231 non-integration Python passes with 2 expected Windows skips, 57/57 web tests, 2/2 contract tests, and 23/23 complete integrations plus production builds, generated DB/OpenAPI drift, policy, secret, SCA, and restore gates. Forward invariants bind frozen run/provider provenance and immutable successful provider receipts; the UI exposes verified receipt provenance without fabricating history. Restore proof derives the checked-in migration head and preserves the source run-control state. Trusted service/environment/release fields overwrite forged log metadata, and worker liveness probes the running process while readiness measures dependencies. A dedicated NOINHERIT/NOBYPASSRLS `simula_operator` login has no password, memberships, elevated attributes, table privileges, or arbitrary schema creation and can execute only audited status/set run-control functions; verified enable requires an explicit recovery guard. Complete-history Gitleaks uses one exact fingerprint suppression for the intentional historical injection canary and reports no leak. Seed-free migration `20260720083000_phase2_operator_run_control.sql` is applied to hosted project `ywiwmczccktwzqyhzhiz`; local/remote histories match, linked lint and security advisors report zero lints, catalog/ACL checks pass, and hosted run creation remains enabled. The bounded independent cross-domain code re-review completes with no unresolved code Critical or High finding. Commits through `72f1a66` are pushed; GitHub Actions `29728979248` passes all four named jobs, including hardened container proof, on exact head `72f1a66cf1a0be8e589f9ef5f88a84eb5cfcb10d`. | OBSERVED | High | This is local/disposable application proof plus hosted schema/ACL evidence. It is not a human screen-reader pass, enforceable required-check policy, hosted application deployment, user-facing deletion orchestration, full application-compatible staging restore, customer-data load, real-provider call, scientific validation, or production-readiness claim. Formal Phase 2 exit remains open and Phase 3 remains blocked. Existing performance-advisor recommendations remain monitored under R-031. | P2-07 remediation, R-023–R-025, R-031, Phase 2 audit |
| E-5034 | PhantomCrowd reference adoption and M1 NestJS/BullMQ control-plane foundation | 2026-07-29 | Plan 003, ADR-0011, ADR-0012, `apps/api`, separate NestJS OpenAPI 3.1 contract, and generated control-plane TypeScript client establish a rollback-safe migration foundation. Frozen pnpm install passes. NestJS passes strict build/typecheck, 20/20 Jest unit/E2E tests, and 2/2 loopback Redis DB-14 BullMQ integration tests covering identifier-only payload, exact job identity, replay deduplication, one transport attempt, and live readiness. Contract generation/drift and 3/3 contract tests pass. Next 16.2.11 regressions pass web 74/74 and admin 2/2 tests, both lint/typecheck gates, and both production builds. Secret scan passes 683 files; forbidden-claim policy passes 118 files; npm/Python SCA exits zero with no known Python issue and one exact ignored brace-expansion advisory isolated to dev-only legacy minimatch paths, absent from web/admin/API production trees. All SIMULA TypeScript workspace lint/typecheck tasks pass. | OBSERVED | High | Local Windows evidence only. Root format remains blocked solely by unchanged tracked/generated `apps/admin/next-env.d.ts`; root Python Ruff/mypy additionally inspect the user's unrelated untracked `scripts/generate_user_guide_pdf.py`, which has existing lint/type errors and was not changed. No NestJS domain mutation, outbox publisher, real AI provider, dataset, hosted resource/schema, production traffic, deployment, commit, or push is included. | Plan 003 M0/M1, ADR-0011, ADR-0012, R-032–R-037 |
| E-5035 | First gated NestJS authenticated-domain read slice | 2026-07-29 | The disabled-by-default `/api/v2/me` and `/api/v2/organizations` migration surface now has strict local/test/preview/staging configuration, an explicit production-enable rejection, asymmetric RS256/ES256 JWKS verification with bounded no-redirect fetch/cache/rotation behavior, local Auth-verified HS256 fallback, canonical UUIDv4/v7 correlation, duplicate Authorization rejection, FastAPI-byte-compatible HMAC cursors, RFC 9457 failures, direct least-privilege PostgreSQL pooling, transaction-local JWT/release claims, RLS-scoped organization reads, domain-aware readiness, OpenAPI 3.1, and regenerated TypeScript contracts. Strict build/typecheck and 52/52 Jest tests pass; 2/2 live Redis/BullMQ tests remain green; 3/3 contract tests and independent generation/drift pass; frozen install and npm/Python SCA pass. The separate PostgreSQL integration gate is authored to rotate/restore the disposable local API password, add/delete one fixture, and prove owner visibility, cross-tenant denial, and claim cleanup. On this run it failed safely before mutation because the process listening on loopback 54322 had no SIMULA schema or fixture users, while Docker control was unavailable. | OBSERVED | High | M2 is not complete. Real reset-driven PostgreSQL/RLS/JWT route proof, authenticated/general rate admission, sign-in audit, remaining v1 domain routes, golden response/error parity, cross-tenant HTTP proof, and browser migration proof remain open. The migration routes cannot be enabled in production. No hosted schema/resource, customer data, production traffic, deployment, commit, or push changed. | Plan 003 M2 partial, ADR-0011, R-032 |
| E-5036 | Expanded NestJS M2 domain, rate, contract, and browser-migration slice | 2026-07-29 | The disabled `/api/v2` surface now covers identity, organizations, projects, stimuli and versions, the authored non-representative demo audience, run create/read/cancel, schema-validated results, provenance, and auth events. Redis ports exact FastAPI token-bucket and accepted/pending idempotency-marker semantics for unauthenticated, authenticated, organization, mutation, run-create, run-read, and cancellation policies; pre-auth admission now precedes media/body parsing and verified requests refund the provisional IP token before user admission and durable sign-in audit. Mutations commit PostgreSQL before rate-marker acceptance, rejection retains retry authority, denied privileged actions use durable audit calls, project mutation uses strict `If-Match`, and run replay bypasses a new rate charge while preserving best-effort queue repair. Exact-origin CORS, 64 KiB strict JSON, safe 413/415/422 problems, W3C trace continuation, UUID correlation, 10s/5s handler deadlines, strict DB row mapping, deterministic result JSON-Schema validation, and fail-closed provenance disclosure are covered. Generated NestJS OpenAPI publishes 13 overlapping domain paths plus health, a generated TypeScript client, no-extra object schemas, and the same stable problem inventory; an executable golden test proves v1/v2 method, status, body-field, response-field, auth, and media-type parity. The Next client moves only reviewed routes behind `NEXT_PUBLIC_SIMULA_DOMAIN_API_VERSION`, defaults to `v1`, rejects unknown values, and leaves nonported product/admin routes on v1. Frozen install, API typecheck/build, 92/92 Jest, 6/6 live Redis/BullMQ integration, OpenAPI drift, 4/4 contracts, web typecheck, 76/76 Vitest, and the Next 16.2.11 production build pass. Formatting for impacted sources, 118-file forbidden-claim policy, and 799-file secret scan pass. SCA initially found AJV 8.17.1 GHSA-2g4f-4pwh-qvx6; exact AJV 8.20.0 removes it, after which npm/Python SCA passes with only the pre-existing exact ignored legacy dev-only high advisory. | OBSERVED | High | This is local code, generated-contract, browser-client unit, build, and live Redis evidence. It is not real PostgreSQL/RLS/JWT HTTP proof, authenticated browser E2E, hosted schema/application deployment, production traffic, BullMQ outbox confirmation/worker equivalence, real-provider execution, customer-data use, scientific validation, commit, or push. Loopback 54322 remains non-SIMULA and Docker control remains unavailable, so M2 stays open, browser default stays v1, and production enablement remains rejected. | Plan 003 M2 partial, ADR-0011, R-032 |
| E-5037 | Partial M3 BullMQ dispatcher and Python worker migration seam | 2026-07-29 | A separate Node dispatcher entrypoint parses only an exact `simula_worker` database URL, rejects production, uses a four-connection transaction-bounded pool, validates the legacy durable outbox job identity, publishes the identifier-only v2 job, follows with an exact retained-job read before compare-and-set confirmation, preserves cancellation/poison/recovery passes, and feeds Redis memory plus poison state to durable run control. The Python worker adds an explicit default-ARQ transport flag, strict queue/name/job/payload/attempt binding, a worker-only v2 claim gateway, bounded database-authorized pre-confirmation/capacity/provider deferrals by moving the active job directly to delayed, a fixed `simula:v2` runtime, queue/readiness metrics, and fail-closed shutdown/supervision. BullMQ-Python is pinned to 2.14.0 because the inspected current 2.26.0 Redis 7.4 requirement conflicts with the retained ARQ/Redis 5 rollback graph; the pinned internal delay sentinel has a real Redis conformance test. Migration `20260729090000` and a four-assertion pgTAP ACL test are checked in. Frozen install passes; NestJS typecheck/build, 121/121 Jest, and 7/7 live Redis tests pass; strict Python formatting/Ruff/mypy over 69 sources pass; 294 non-integration Python tests pass with 2 expected Windows skips; the real Python BullMQ deferred-redelivery/runtime-snapshot test passes; contract build/drift, 120-file claim policy, 825-file secret scan, and npm/Python SCA pass with only the pre-existing exact ignored legacy dev-only high advisory. | OBSERVED | High | Local code/unit/live-Redis evidence only. The v2 SQL wrapper and pgTAP file are not reset-tested because loopback 54322 is non-SIMULA and Docker control is unavailable. Real database-backed duplicate/crash/stall/cancel/poison/stale-lease/Redis-loss/backpressure/one-terminal-result equivalence is absent. Python BullMQ is pre-stable and uses a pinned internal active-to-delayed seam. ARQ remains default; NestJS, dispatcher, and BullMQ worker production admission all fail closed. No hosted schema/resource, traffic, deployment, customer data, provider call, commit, or push changed. | Plan 003 M3 partial, ADR-0011, R-020, R-032 |
| E-5038 | Partial M4 governed behavioral engine, private service, and durable experimental execution seam | 2026-07-29 | `simula_core.behavioral_engine` independently implements the ADR-0012 decomposition: strict provenance/checksum context graphs; admitted population sampling; evidence-bound psychographic cohorts; deterministic weighted LLM/rule fleet manifests and peer relationships; seeded provider requests; bounded interaction rounds, immutable action events, run-scoped relationship memory, replayable crowd pulse, typed heuristic scores, explicitly non-population synthetic dispersion, evidence-bound qualitative synthesis, labelled synthetic interviews, matched A/B deltas without winner/lift claims, and tamper-replay receipts. The private FastAPI service has exact no-fallback provider admission, rotating bearer authority, strict JSON/schema/2 MB boundaries, cancellation/deadline/cost mapping, health/readiness, and a pinned non-root container. The worker client restricts origins, redirects, proxies, encoding, size, schema, and command binding; a real loopback Uvicorn integration proves the HTTP boundary. A compact frozen `behavioral_demo_run_v1` schema-v2 manifest now routes only through BullMQ, reconstructs the fixed synthetic demo command, and binds exact canonical result bytes to a run/attempt/lease receipt. Migration `20260729094522_behavioral_engine_artifacts.sql` defines forced-RLS summary/private-payload/provider-receipt tables, an API-only idempotent experimental admission command, and one worker-only atomic completion that persists results, receipts, events, audit, and terminal state. NestJS publishes the dedicated `/api/v2/projects/{project_id}/behavioral-demo-runs` route and preserves BullMQ job identity through cancellation; OpenAPI and generated clients include it. A second authenticated `/api/v2/runs/{run_id}/behavioral-result` route returns only the tenant-filtered report summary and checksums; the 16 MB canonical payload stays private. The report uses a generated Python-authority JSON Schema plus semantic action-share, score, and evidence-binding validation. `behavioral_evaluation` adds a prespecified benchmark-only harness with disjoint development/holdout IDs, exact-row and method enforcement, complete baseline comparison, deterministic outcome checksum, constant-score disclosure, and sparse-subgroup suppression. Complete non-integration Python passes 359 with 2 expected Windows skips and 27 integrations deselected; NestJS passes 131/131; web passes 76/76; contracts pass 5/5; all 9 TypeScript workspace lint/typecheck tasks, changed-slice Ruff/mypy, 124-file forbidden-claim policy, generated drift, and static SQL parsing pass. | OBSERVED | High | Local code, mock/live-loopback HTTP, and static SQL evidence only. The new migration and 18-assertion pgTAP boundary are not reset-tested because loopback 54322 is not a disposable SIMULA database and Docker control is unavailable. Database-backed duplicate/crash/cancel/replay/deletion proof is absent. The evaluation harness has no rights-cleared outcome corpus and establishes no validity. The worker adapter remains disabled and production-rejected. No external provider or egress, held-out human/outcome evaluation, validated score, hosted resource, staging/production deployment, traffic, customer data, commit, or push is included. M4's local implementation slice is complete, but its database/provider/evaluation release gates remain open and every output remains experimental. | Plan 003 M4 partial, ADR-0012, R-033–R-037 |
| E-5039 | Partial M5 governed behavioral data, evidence-rights, outcome, asset, and benchmark registry | 2026-07-29 | Three CLI-created additive migrations define a normalized execution-evidence graph, evidence-source/version rights metadata, aggregate-only observed campaign outcomes, private stimulus-asset metadata and bucket configuration, and versioned benchmark protocols/runs/members. Canonical behavioral payload insertion atomically normalizes tenant-bound context graphs, fleet manifests, action events, run-scoped memories, and report-to-event links; the canonical byte payload remains replay authority. Every application table has forced RLS and least-privilege grants. Aggregate subgroup outcomes require at least 50 observations. Admitted outcomes require current approved source rights. The private 16 MB asset bucket has a fixed MIME allowlist, no browser object policy, and server-mediated metadata authorization; no asset bytes are stored in application tables. Benchmark rows can only use the `benchmark_only` label and bind predictions, admitted observations, protocol splits, and provenance checksums. Vector columns/indexes remain intentionally absent until an admitted embedding model, recall corpus, and query-plan budget exist. The three new pgTAP files contain 49 assertions, and the 35-assertion exact catalog inventory is updated. Seven changed migration/test SQL files parse statically. Complete non-integration Python passes 359 with 2 expected Windows skips and 27 integrations deselected; NestJS passes 131/131; web passes 76/76; contracts pass 5/5; all 9 TypeScript workspace lint/typecheck tasks, changed-slice Ruff/mypy, generated contracts, 124-file claim policy, and 855-file secret baseline pass. | OBSERVED | High | Schema, static pgTAP, and code-regression evidence only. Docker's Linux engine is unavailable and loopback 54322 is not SIMULA, so migrations, triggers, backfill, RLS, storage bucket behavior, cascades, generated database types, and all 49 new pgTAP assertions remain unexecuted. No rights-cleared outcome corpus, uploaded asset, embedding model/vector benchmark, hosted mutation, provider call, deployment, customer data, commit, or push is included. M5 remains open. | Plan 003 M5 partial, R-003, R-012, R-031, R-033, R-038 |
| E-5040 | Partial M6 governed campaign-lab workflow | 2026-07-29 | The authenticated campaign slice now spans feature-gated behavioral run launch, live run status, strict result rendering, governed context evidence, public fleet composition, one-to-five-round interaction timeline, ten explicitly synthetic/not-testimony interview replays, recommendations/limitations, and matched retest/A/B comparison with validated JSON export. Migration `20260729110611_m6_behavioral_public_summaries.sql` atomically derives forced-RLS public fleet/round/agent summaries from canonical payload insertion without exposing traits, dimensions, rationale, memory, or private actions; its focused pgTAP file contains 16 assertions and the exact database catalog is updated. The evidence route validates fleet/round/event/context/report checksums and returns fixed bounded summaries. The comparison route requires different variants with the same organization, study, methodology, provider/version/model/template, provider-call budget, context graph, fleet checksum, and ordered synthetic agent IDs; it reports candidate-minus-baseline deltas, `winner: null`, and no lift, causal, human-preference, or population claim. Python authority generates the context, evaluation, report, and comparison schemas. NestJS passes 154/154 across 27 suites; web passes 98/98 across 28 files plus TypeScript, ESLint, and optimized Next.js build; contracts pass 7/7; complete non-integration Python passes 371 with 2 expected Windows skips and 27 integrations deselected; SQL parses statically; generated-contract drift, observability assets, Ruff, and mypy pass. | OBSERVED | High | Local unit/static/build evidence only. The M5/M6 migrations and pgTAP assertions are not reset-tested because Docker Linux remains unavailable and loopback 54322 is not SIMULA. Therefore no real database trigger/backfill/RLS/HTTP/browser journey, deletion, or export download is claimed. Audience creation remains the governed demo-audience path, not a rights-cleared population builder. Desktop/mobile Playwright, keyboard, human screen-reader, Axe, visual/responsive, loading/empty/error, and audit-history proof remain open. The production feature gate stays off. No hosted mutation, provider call, deployment, customer data, commit, or push is included. | Plan 003 M6 partial, R-023, R-032, R-037, R-038 |
| E-5041 | Partial M7 privacy-bounded Sentry/OpenTelemetry operations layer | 2026-07-29 | NestJS API/dispatcher starts pinned Sentry 10.69.0 and OpenTelemetry Node SDK 0.221.0 before application modules; Python API/worker/AI-engine share pinned Sentry SDK 2.66.1 and OpenTelemetry SDK 1.44.0 with FastAPI/HTTPX plus explicit worker-job and behavioral-execution spans; Next.js 16 initializes server, edge, and browser Sentry error capture. Export is disabled by default and requires exact environment/release identity, both Sentry and OTLP HTTP/protobuf endpoints, bounded sampling, and HTTPS outside local loopback. Sentry performance export is disabled; OpenTelemetry is trace authority. Redaction tests prove request, user, URL, body, header, breadcrumb, arbitrary context/extra, transaction, and exception-message removal. Span exporters replace names with bounded operation classes and retain only a fixed low-cardinality operational attribute allowlist. A checked immutable Grafana dashboard defines 8 panels; 6 symptom-based Prometheus alerts link runbooks and avoid tenant/user/project/run/agent labels. `pnpm observability:check`, 156 NestJS tests, 107 web tests, 371 Python tests, Next build, Node/Python peer/type/lint checks, a current 1,748-text-file secret scan, Python SCA, and npm Moderate gate pass; npm reports only the pre-existing explicitly ignored dev-only advisory. | OBSERVED | High | No Sentry project, OTLP collector, Prometheus/Grafana import, alert receiver, staging deployment, real exported error/span, redaction inspection, alert firing/recovery, on-call acknowledgement, load/cost proof, restore/rollback drill, signed release, or production change is included. Source-map upload remains off unless all three build credentials are supplied; the Sentry CLI and protobuf install scripts remain explicitly denied. M7 and production readiness remain open. | Plan 003 M7 partial, R-024, R-025, R-039 |
| E-5042 | Local deterministic behavioral-engine envelope rehearsal | 2026-07-29 | `pnpm behavioral:capacity` executes repeatable authored synthetic fixtures on Python 3.14.6/Windows 11 with `tracemalloc` active. Ten 10-agent/1-round runs recorded p95 0.055513s, maximum 21,784 canonical bytes, and peak traced allocation 181,126 bytes. Five 200-agent/3-round runs recorded p95 1.157815s, maximum 640,671 bytes, and peak 5,425,300 bytes. Two maximum-envelope 2,000-agent/5-round runs recorded p95 24.005537s, maximum 9,480,823 bytes against the 16,000,000-byte result cap, and peak 80,999,780 bytes. Every run bound provider calls exactly to agents times rounds, cost exactly 0 microusd, and produced one stable output checksum per repeated case. Script budgets are 1s/10s/60s and 64/256/768 MiB respectively and fail closed on timing, allocation, size, call-count, cost, or determinism drift. | OBSERVED | Medium | Sequential single-process local deterministic-provider rehearsal only, with 10/5/2 samples and Python allocation tracing; it is not concurrent service load, database/queue/network capacity, hosted infrastructure sizing, a production SLO, a billable-provider cost forecast, or customer workload evidence. Staging concurrency, queue/backpressure, database plans, autoscaling, telemetry overhead, billable-provider reservations, and sustained soak remain M7 gates. | Plan 003 M7 partial, R-033, R-034 |
| E-5043 | Partial M7 target manifests and fail-closed release provenance | 2026-07-29 | Added a pinned non-root NestJS control-plane image, preview/staging Railway control-plane manifest, private AI-engine manifest, and web build arguments for public environment/release/Sentry configuration. The control-plane build runs the full peer check before a production-only pnpm deployment; an isolated local deployment assembled `dist`, workspace contracts, Sentry, and OpenTelemetry runtime dependencies and loaded the runtime packages successfully. Nine deployment tests validate rollout windows, readiness paths, pinned build contexts, non-root/telemetry-first startup, public web build values, the release contract, and a no-dual-write/no-dual-consume staged rollback sequence. Two restore-contract tests bind the drill to migration head `20260729113000` and require row-count discovery for every current `api` and `private` table. A new tag/manual workflow is disabled until `SIMULA_ATTESTATIONS_ENABLED=true`; it uses exact toolchains and frozen locks, runs release/security/observability/capacity gates, builds Python wheels, creates a timestamp/owner/order-normalized gzip bundle, writes SHA-256 checksums, invokes pinned `actions/attest` commit `f7c74d28b9d84cb8768d0b8ca14a4bac6ef463e6` (`v4.2.0`), immediately verifies with `gh attestation verify`, and uploads only after verification. | OBSERVED | Medium | Docker Desktop's Linux server still returns HTTP 500/no server, so no new image was built or scanned. The updated restore drill is unexecuted. The Railway/Vercel/Supabase manifests are not deployed. The GitHub workflow is authored but unexecuted; no tag, attestation, signature, artifact, workflow run, repository variable, Enterprise-plan support, commit, push, release, or production change exists. NestJS production admission remains deliberately rejected pending database/browser/recovery equivalence. | Plan 003 M7 partial, R-025, R-032 |
| E-5044 | M7 repository privacy/security review with production rejection | 2026-07-29 | The implementation review maps browser/Sentry, service/Sentry, OTLP, Supabase Auth/PostgreSQL/Storage, worker/private-engine, and external-provider boundaries; enumerates permitted/prohibited fields; reviews telemetry leakage, cross-tenant behavioral evidence, synthetic-output misinterpretation, provider/prompt abuse, and supply-chain/release identity; and binds each control to current implementation evidence. It confirms default-off vendor export and external provider, final-export redaction, fixed metric attributes, private canonical action/memory/rationale/assets, checksum-bound public summaries, explicit synthetic/no-winner claims, pinned dependencies/actions, and fail-closed release provenance. Its approval checklist marks technical repository review complete and rejects production. | INFERRED | Medium | This is an internal technical review, not legal advice, privacy-law compliance, vendor approval, penetration testing, independent security audit, or production authorization. Named privacy/legal/security approvers, Sentry/collector data terms/region/retention/RBAC/deletion, hosted redaction, disposable RLS/storage/browser/deletion proof, external-provider assessment, restore/rollback/incident drills, and formal go/no-go remain unchecked. | Plan 003 M7 partial, R-005, R-006, R-007, R-039 |
| E-5045 | Partial M6 immutable refinement/retest, sanitized run audit history, and fixture-backed browser QA | 2026-07-29 | A new user-visible refinement panel creates an immutable stimulus revision and launches a separate governed behavioral run. Its coordinator resolves the exact source stimulus, supplies independent stable append/run idempotency keys, retains the created version after an ambiguous run-creation failure, and reuses all prior durable progress on retry; tests prove no duplicate append. The run workspace checks organization mutation permission before exposing the form. Migration `20260729113000_m6_run_audit_history.sql` adds a stable, empty-search-path, security-definer authority and invoker wrapper that require the `simula_api` session plus tenant membership and return at most 50 newest-first `private.run_events` projections. The projection contains only state transition, attempt, safe reason, actor class, correlation, and timestamp; actor user ID, free metadata, audit rows, payloads, prompts, agent memory, and rationale are not selected. The NestJS route uses existing run-read admission; strict DTO/OpenAPI/Zod contracts reject extra fields, wrong run identity, duplicates, or noncanonical order. The UI renders the timeline chronologically and labels the privacy boundary. The migration and 14-assertion pgTAP file parse statically. Local gates pass 156/156 NestJS tests, 107/107 web tests across 33 files plus TypeScript, ESLint, and optimized Next build, 7/7 contracts with generated drift, and 371 Python tests with 2 expected Windows skips and 27 integrations deselected. A loopback-only browser fixture used an ephemeral ES256 session verified by the real Supabase SSR `getClaims()` JWKS path and served strict domain contracts. Real Next/browser code rendered the full report, public evidence, audit timeline, and refinement permission; keyboard submission made one immutable-version POST and one retest POST before navigating to the queued run; matched comparison and downloaded JSON retained `winner: null`; an injected audit 503 hid the entire report and recovery restored it. Final desktop/mobile captures had no console errors. Axe 4.12.1 reported 0 violations with 48 desktop and 49 mobile passes after correcting definition-list semantics and making the horizontally scrollable timeline keyboard-focusable. Four mobile contrast checks remained automation-incomplete only because clipped scroll content obscured background detection; inspected color pairs measured 5.70:1 and 6.11:1. | OBSERVED | High | The browser evidence is fixture-backed presentation/client-contract evidence, not a database-backed E2E or human assistive-technology study. Migration `20260729113000`, its 14 pgTAP assertions, and the underlying M5/M6 schema are not executed because Docker Linux remains unavailable and loopback 54322 is not SIMULA. Real tenant/RLS/HTTP, ambiguous-network replay against durable storage, deletion, loading/empty-state, and human screen-reader proof remain absent. The current audience is still a governed non-representative demo cohort, not a rights-cleared population. The production feature flag stays off; no hosted mutation, provider call, deployment, customer data, commit, or push is included. | Plan 003 M6 partial, R-023, R-032, R-037, R-038 |
| E-5046 | Governed pgvector storage and exact graph retrieval seam | 2026-07-29 | CLI-created migration `20260729132200_m7_governed_context_embeddings.sql` installs pgvector in `extensions`, creates a private embedding-model registry and private context-node vector store, forces RLS, and grants no direct browser/API/worker table access. Registry admission requires typed artifact, rights, prohibited-use, review, benchmark, query-count, semantic-relevance, exact-recall, and lifecycle evidence. Vectors bind organization, immutable graph version, node ID, node-content checksum, model version, and a database-derived vector checksum. A worker-owned definer accepts only admitted models and exact dimensions/normalization, then performs immutable idempotent insert or rejects conflicting retries. A command-owned definer requires the `simula_api` session plus tenant membership and performs bounded exact cosine search over one graph; no ANN index or unverified iterative-scan assumption is used. The migration seeds no model and no vectors. Twenty-one focused pgTAP assertions plus exact database catalog, ACL, function, policy, foreign-key, empty-fixture, and restore-head inventories are authored. | OBSERVED | Medium | Source/schema and static catalog-test evidence only. Docker Linux remains unavailable and loopback 54322 is not SIMULA, so migration execution, pgvector version/location, CHECK constraints, forced-RLS behavior, vector ingestion, cross-tenant denial, exact retrieval, query plan/latency, generated database types, restore drill, and all assertions are unexecuted. No rights-cleared model/corpus, model admission, embedding generation, vector row, HTTP/OpenAPI product route, hosted mutation, provider call, deployment, customer data, commit, or push exists. This implements a fail-closed seam, not semantic quality, behavioral validity, Predikta equivalence, or production readiness. | Plan 003 M5/M7 partial, ADR-0013, R-003, R-012, R-033, R-038 |

| E-5047 | M2 Phase 3/4 methodology workflow parity and private numerical authority | 2026-07-29 | NestJS `/api/v2` now serves the authenticated methodology registry, organization audience create/list, project simulation-configuration create/list, and project methodology preview. Strict nested DTOs reject unknown or malformed input; existing least-privilege RLS transactions and atomic `api.create_audience_definition` / `api.create_simulation_configuration` functions retain durable authority and replay behavior. Synchronous preview loads only tenant-visible frozen configuration/stimulus/run state, admits only the zero-cost deterministic provider, and sends a bounded normalized command to a bearer-authenticated private FastAPI endpoint. The Python `MethodologyEngine` and `build_complete_report` remain the sole numerical/report authorities. The NestJS adapter rejects redirects, encoded/oversized/malformed or identity-mismatched results and requires experimental/heuristic labels. JavaScript preview/report UUIDv5 identities byte-match Python. Generated Nest OpenAPI and TypeScript contracts include the five-route workflow; the Next client uses generated v2 input/response types and routes it through the existing default-v1 rollback flag. The existing Nest regression suite passes 157/157, focused new adapter/identity/config/readiness tests pass 22/22 across two runs, Python engine/observability tests pass 10/10, golden v1/v2 contracts pass 7/7, and the web API client passes 12/12. Nest TypeScript, generated-contract drift, web TypeScript/ESLint/optimized build, Ruff, mypy, the 149-file claim scan, and the 1,762-file secret scan pass. | OBSERVED | High | Local code/unit/contract evidence only. No disposable SIMULA PostgreSQL is available, so the existing/new SQL authorities, RLS, cross-tenant denial, replay, linked succeeded-run check, and complete HTTP/browser workflow are not executed in this slice. The browser stays on v1 by default and NestJS production admission remains rejected. The deterministic provider remains an experimental heuristic rehearsal fixture; no rights-cleared population, human/outcome validation, Predikta equivalence, hosted provider, deployment, commit, push, or production-readiness claim is included. | Plan 003 M2 partial, ADR-0011, ADR-0012, R-032, R-033, R-037 |
| E-5048 | M2 durable campaign-optimization loop parity | 2026-07-29 | NestJS `/api/v2` now serves variant-group create/list, variant-report comparison, succeeded-run methodology-report create/read, report-export create, and active export download. Strict DTOs bound group size/order, identifiers, labels, formats, and timezone-aware expiry. Existing RLS transactions and atomic `api.create_variant_group`, `api.create_report_artifact`, and `api.create_report_export` commands remain durable/idempotency authorities. Report construction still uses the E-5047 private methodology path; new bearer-authenticated private FastAPI endpoints exclusively execute `compare_variants` and `export_report`. Incompatible frozen configurations return 409 and never produce a winner. NestJS rejects redirects, encoded/oversized/malformed responses and rechecks comparison report identities plus export format, media type, filename, canonical base64, byte limit, and SHA-256 before SQL. Comparison alone has a bounded 9,000,000-byte command limit so eight valid maximum-size reports remain usable; other private commands stay at 2,000,000 bytes. Download rechecks stored-byte SHA-256 and emits attachment, ETag, no-store, nosniff, exact media type, and length headers. Generated OpenAPI/client types and the Next default-v1 rollback seam cover seven new route pairs; arbitrary user-supplied report upload is intentionally absent from v2. Full NestJS tests pass 167/167 across 29 suites, Python core/private-engine tests 155/155, web tests 108/108 across 33 files, and golden contracts 7/7. Nest typecheck/build/OpenAPI drift, contracts build/drift, web ESLint/optimized build, Ruff, mypy over 35 Python source files, 149-file claims, 1,768-file secrets, targeted formatting, and `git diff --check` pass. | OBSERVED | High | Local code/unit/contract/build evidence only. No disposable SIMULA PostgreSQL is available, so the existing Phase 4 SQL commands, RLS, role enforcement, idempotent replay/conflict, report uniqueness, expiry, bytea persistence, cross-tenant denial, real HTTP downloads, and authenticated browser workflow are unexecuted in this slice. Root format remains blocked only by unchanged tracked generated `apps/admin/next-env.d.ts`; impacted files pass formatting. The browser stays on v1 by default and production NestJS admission remains rejected. No rights-cleared population, held-out human/outcome validation, causal lift, vendor equivalence, hosted provider, deployment, customer data, commit, push, or production-readiness claim is included. | Plan 003 M2 partial, ADR-0011, ADR-0012, R-032, R-033, R-037 |
| E-5049 | M6 durable methodology report, verified export, and variant-comparison browser workflow | 2026-07-29 | The Next run workspace now exposes a methodology-report panel only for succeeded schema-v2 behavioral runs. It lists frozen configurations, treats an absent report as an expected creation state, binds created and loaded run/report/artifact identities, and displays experimental validation/output/checksum disclosure. JSON/CSV export creation crosses the existing v1/v2 rollback seam; the browser download boundary accepts only exact JSON or CSV media type, a safe attachment filename, at most 2 MiB declared and actual bytes, and a quoted 64-hex ETag whose SHA-256 matches Web Crypto before creating an object URL. API tests cover successful download, unsafe filename rejection, and ETag mismatch. Methodology Lab now loads durable variant groups, reloads after creation, requests group comparison, and renders the largest modeled change with explicit no-winner/no-causal-market-lift text. Full web regression passes 113/113 tests across 35 files; ESLint, focused Prettier, optimized Next 16.2.11 build/TypeScript, 152-file forbidden-claim scan, 1,790-file secret scan, and `git diff --check` pass. A loopback-only fixture with an ephemeral ES256/JWKS Supabase session exercised the real Next/Supabase client: report create, JSON export/download, persisted clean reload, saved two-member group listing, comparison, desktop/mobile rendering, and final consoles with 0 errors/0 warnings. Downloaded report SHA-256 was `c7feda900491604773a0a5f75ac7c7d98d897cfd8a20e009c5b66842057e00c9`; the 390x844 comparison viewport had `scrollWidth=innerWidth=390`. Four screenshots are retained under `output/playwright/e5049-*.png`; ephemeral auth state, fixture source, and downloaded artifact were removed after evidence capture. | OBSERVED | High | Browser evidence is fixture-backed client/presentation-contract proof, not NestJS/private-engine/PostgreSQL/RLS E2E. It does not execute durable SQL replay/conflict, report uniqueness, expiry, bytea persistence, cross-tenant denial, real HTTP limits, deployment, customer data, or production admission. First-create 404 is an expected domain state; the final persisted reload was the clean-console proof. No Axe run or human assistive-technology review was performed for this slice. The v1 browser route remains the default outside the explicit v2 test configuration. No rights-cleared population, held-out human/outcome validation, causal lift, vendor equivalence, hosted provider, commit, push, or production-readiness claim is included. | Plan 003 M2/M6 partial, ADR-0011, ADR-0012, R-023, R-032, R-037 |
| E-5050 | M5 private stimulus reservation, verified S3 transport, and durable deletion seam | 2026-07-29 | CLI-created migration `20260729151639_m6_private_stimulus_asset_pipeline.sql` makes expected media, byte size, lowercase SHA-256, retention, and `{organization}/{stimulus}/{asset}/{digest}` path immutable; safely backfills content-bearing legacy rows and stops on unresolved pending legacy rows; extends exact command-receipt scopes; and adds API-only reservation, upload confirmation, deletion request, and deletion confirmation functions with owner/editor checks, audit events, fixed search paths, RLS-on definers, least-privilege execution, replay-safe reservation/deletion, and two-phase tombstoning. Twenty-one focused pgTAP assertions plus the exact global function/owner/ACL inventories are authored. NestJS adds five authenticated v2 operations, strict JSON versus exact binary media handling, 16 MiB limits, tenant reads, retention rejection, S3 metadata/byte/hash rechecks, replay-safe object reuse, private no-store/sandbox/nosniff downloads, and object removal/absence confirmation before tombstone. Optional runtime configuration admits only loopback local S3 or the direct deployed Supabase Storage hostname and joins readiness only when enabled. The S3 adapter independently restricts the sole bucket/key namespace, safe filename, media/size/hash metadata, actual bytes, timeouts, and missing-object behavior; the port preserves a future equivalent R2 seam. Public DTOs exclude bucket and object path. OpenAPI exposes the exact five operations/media envelope and generated TypeScript nullable primitives correctly. Full local regression passes 31 API suites / 186 tests, 35 web files / 113 tests, 2 admin tests, 7 contract tests, 374 Python tests with 2 expected Windows skips and 27 integrations deselected, plus 3 development-loader tests. All four production builds, generated contract drift, TypeScript workspace lint/typecheck, focused formatting and diff checks, 152-file forbidden-claim policy, 1,807-file secret scan, npm Moderate admission with one exact ignored legacy dev-only High advisory, and Python SCA with no known vulnerabilities pass. | OBSERVED | High | Database execution is absent: local `supabase db lint` and pgTAP both exit 1 before tests because Docker/Supabase PostgreSQL cannot enable `pgsql_check` or pgTAP. Migration syntax/catalog behavior, all 21 assertions, forced-RLS/cross-tenant behavior, generated database types, restore drill, and the legacy-row stop are therefore unverified at runtime. S3 tests mock the AWS client; no real Supabase S3 credential, object, retention expiry, delete recovery, cross-bucket denial, hosted resource, browser workflow, or R2 adapter was exercised. The exact AWS release train has narrow release-age exceptions pending expiry. This is governed byte ingestion only: no OCR, image/video understanding, multimodal model, behavioral analysis, population evidence, Predikta equivalence, deployment, customer data, commit, push, or production-readiness claim exists. | Plan 003 M5 partial, ADR-0014, R-005, R-012, R-032, R-038 |
| E-5051 | M5/M6 private stimulus asset client, safe preview, and fixture-backed browser lifecycle | 2026-07-30 | A server-owned `SIMULA_PRIVATE_ASSET_WORKFLOW_ENABLED` switch fails closed unless the browser domain API is also v2. The generated-contract client lists, reserves, uploads, downloads, and deletes only through the five E-5050 operations. Before reservation the UI enforces the exact PDF/JPEG/PNG/WebP/MP4 and 16 MiB envelope and computes lowercase SHA-256 with Web Crypto. Reserve and upload idempotency keys survive ambiguous retries without duplicate reservation; deletion has a separate stable retry key and an explicit destructive confirmation. The exact public response parser accepts only the generated field set, rejects bucket/object-path leakage, validates identifiers/dates/media/size/hash, and binds pending/available lifecycle consistency. Available access requires exact media type, inline safe filename, declared and actual length, quoted matching SHA-256 ETag, private no-store, sandbox, and nosniff headers, then recomputes SHA-256 before creating a blob URL. Images preview locally, PDFs render in a sandboxed iframe, and MP4 downloads after verification. Blob URLs are revoked on close/delete/unmount. Viewer roles can list and access but cannot attach/delete. Visible copy states that storage does not analyze, interpret, or score contents. CORS exposes only the response headers required for this verification. Tests cover exact parsing/private-field rejection, route/header/body behavior, tamper rejection, stable retry, preview, viewer mode, and confirmed deletion. Local evidence passes 37 web files / 126 tests, 31 API suites / 186 tests, web/API ESLint/typecheck/build, generated contract drift, focused formatting, 156-file claim policy, 1,817-file secret scan, and a self-starting real-Chromium fixture executing list-reserve-upload-download-delete in order with zero Axe 4.12.1 violations. | OBSERVED | High | The browser proof renders the real React component but injects an in-memory client; it is presentation/workflow evidence, not authenticated Next/Supabase client, NestJS HTTP, PostgreSQL/RLS, or Supabase S3 E2E. Local Auth and the disposable database were unavailable. No real object, expiry boundary, cross-tenant denial, durable network replay, S3 deletion recovery, cross-bucket denial, R2 adapter, mobile/human assistive-technology pass, hosted resource, deployment, customer data, commit, or push exists. The flags remain off by default. No OCR, image/video understanding, multimodal provider, behavioral interpretation, outcome validity, Predikta equivalence, or production-readiness claim exists. | Plan 003 M5/M6 partial, ADR-0014, R-005, R-012, R-023, R-032, R-038 |
| E-5052 | M5/M6 asset-bound technical image profile | 2026-07-30 | ADR-0015 and `SIMULA_TECHNICAL_VISUAL_PROFILE_ENABLED=false` define a fail-closed first visual-methodology seam. Only verified, retained, available JPEG/PNG/WebP assets are admitted. NestJS privately refetches the object and rechecks media type, declared/actual bytes, and SHA-256 before a bearer-authenticated binary request. The Python engine uses Pillow 12.1.0 with an explicit format allowlist, verify/reopen, pre-load 40-megapixel and 16 MiB limits, decompression-bomb rejection, EXIF orientation normalization, and deterministic at-most-256-by-256 sampling. Its exact immutable result contains dimensions, sampling receipt, nine normalized technical signals, provider/method/version, checksum, experimental label, and false behavioral-interpretation, population-inference, and embedded-metadata-retention flags. No OCR, object labels, semantic meaning, emotion, persuasion, aesthetics, or behavioral outcome is produced. Authenticated v2 create/read routes use an asset-stable analysis identity, durable command idempotency, exact response/identity/signal binding, and one immutable forced-RLS profile per immutable asset; a conflicting profile fails closed, and asset retirement removes the derived profile. Generated OpenAPI/client contracts and the private-asset web panel display exact provenance, signals, and fixed limitations without bytes or storage coordinates. Local gates pass 33 API suites / 197 tests, 387 Python tests with 2 expected Windows skips and 27 integrations deselected, 26 focused core/private-engine tests, 7 contract tests, API/web TypeScript, ESLint, both production builds, generated drift, focused Ruff/mypy/formatting, 159-file claims, 1,835-file secrets, and `git diff --check`. Combined web regression covers all 38 files / 131 tests: the first single-fork run passed 84 tests in 26 files but Windows timed out starting 12 untouched workers; those exact 12 files then passed 47/47 using one thread worker. A self-starting Chromium fixture exercised attach, verification, technical profiling, private preview, and deletion with zero Axe 4.12.1 violations. | OBSERVED | High | The migration and 18 pgTAP assertions are authored but unexecuted: local database lint exits before analysis because it cannot enable `pgsql_check`, and local pgTAP exits before tests because it cannot enable the extension. Generated database types, forced-RLS/cross-tenant denial, immutable conflict/replay, real object retrieval, retirement cleanup, and restore proof remain unverified. Browser proof injects an in-memory client and is not authenticated Next/NestJS/PostgreSQL/Supabase-S3 E2E. Repository-wide Ruff and mypy remain blocked only by the unrelated user-owned `scripts/generate_user_guide_pdf.py` with 103 lint issues and 23 typing/stub issues; impacted Python passes focused gates. PDF/video analysis, OCR, object/brand recognition, semantic interpretation, emotion, persuasion, aesthetics, real multimodal provider, rights-cleared audience, human/outcome validation, hosted resource, deployment, customer data, commit, push, Predikta equivalence, and production readiness are absent. All visual flags remain off by default. | Plan 003 M5/M6 partial, ADR-0014, ADR-0015, R-005, R-012, R-023, R-032, R-038 |

| E-5053 | M5/M6 disposable PostgreSQL and private-object technical-profile proof | 2026-07-30 | An isolated PostgreSQL 17 database accepted the complete migration chain from zero. All 13 pgTAP files pass 244/244 assertions; the visual slice includes 18 exact schema/function/RLS/ACL assertions and 14 adversarial replay, immutable-conflict, duplicate-audit, cross-tenant, missing-claim, retirement-cleanup, and failed-command receipt assertions. Clean execution exposed and corrected an original composite-FK ordering defect, invalid schema-qualified SQL syntax, migration owner-context drift, missing asset/profile audit-policy actions, trigger-authority drift, and an unnecessary profile-row update lock that violated least privilege. A loopback RS256/JWKS Supabase-shaped session then executed organization, project, stimulus, private asset reservation, AWS-SDK S3-compatible upload, verified HTTP download, private FastAPI/Pillow profiling, PostgreSQL persistence, exact replay, and read through the actual NestJS routes with isolated Redis. Asset SHA-256 `431ced6916a2a21a156e38701afe55bbd7f88969fbbfc56d7fe099d47f265460` binds to profile checksum `23cba6b384cefe7f8c0b35aeba3844ebd089f355de4b6aae8c904bb5bb23f03e`, nine signals, `technical_image_signals_only`, false behavioral/population claims, one durable command receipt, and one success audit event. The live path exposed strict validation rejecting the empty create DTO; the request now requires exact `technical_image_signals_v1`, and NestJS, Next, generated OpenAPI/types, and tests agree. Current gates pass 34 API suites / 201 tests, 3 focused web files / 28 tests, 27 core/private-engine tests, and 7 contract tests; API/web lint, API build/OpenAPI drift, and generated contract drift pass. | OBSERVED | High | This is disposable local evidence. Moto exercised the real S3 adapter and exact `/storage/v1/s3` shape but is not hosted Supabase Storage. PostgreSQL did not reproduce exact Supabase extension/linter packaging; Docker remains unavailable because firmware virtualization is disabled. The real Next browser did not connect to this stack. Hosted TLS/CORS, cross-tenant HTTP, retention expiry, deletion recovery, human assistive-technology review, hosted telemetry, generated database-type drift, backup/restore, deployment, and production admission remain open. All flags remain off. No customer data, rights-cleared audience, held-out human/outcome validation, behavioral accuracy, Predikta equivalence, or production-readiness claim exists. | Plan 003 M5/M6 partial, ADR-0014, ADR-0015, R-005, R-012, R-023, R-032, R-038 |
| E-5054 | Repeatable NestJS PostgreSQL visual-profile integration | 2026-07-30 | `apps/api/tsconfig.spec.json` now compiles explicit `test/**/*.ts` sources and exposed a stale test runtime fixture missing the required private-engine origin/token, which is corrected with loopback-only values. `organization-gateway.database-int.ts` now uses the real least-privilege `simula_api` pool to create a project, stimulus, reserved/confirmed image asset, and immutable visual profile. It proves first creation, exact idempotent replay, owner read, foreign-tenant null concealment, one durable `stimulus_visual_profile.create` receipt, one `stimulus_visual_profile.created` success audit, asset deletion-request transition, and physical derived-profile erasure. A newly initialized PostgreSQL 17 database accepted all 47 ordered migrations; the explicit database suite passed 4/4, including prior organization visibility, sign-in audit deduplication, and pooled transaction-claim cleanup. The cluster and bootstrap files were removed after proof. | OBSERVED | High | The test is opt-in and requires `SIMULA_TEST_ADMIN_DATABASE_URL` targeting a clean disposable SIMULA database. It exercises the Nest gateway/database boundary, not object bytes, FastAPI, Next, hosted Supabase, or production credentials; E-5053 separately covers the local object/engine HTTP path. No deployment, hosted admission, behavioral validation, Predikta equivalence, or production-readiness claim exists. | Plan 003 M5/M6 partial, ADR-0015, R-032, R-038 |
| E-5055 | Authenticated Next-to-Nest asset lifecycle and dashboard migration proof | 2026-07-30 | A real Chromium session connected the production Next standalone build to a loopback Supabase-shaped RS256/JWKS Auth boundary, the actual NestJS v2 control plane, a clean 47-migration PostgreSQL database, isolated Redis, the real AWS-SDK S3 adapter against an in-memory S3-shaped object boundary, and the private FastAPI/Pillow engine. The browser signed in, created an organization, project, immutable stimulus, and bounded authored-demo run, loaded the project through the v2 organization dashboard, uploaded a 4,277,838-byte PNG, verified the exact SHA-256 `3494290e72c424189d02428baa75997b56b856d69a64530c2156c06c047b8204` before private preview, created the immutable `technical_image_signals_v1` profile, rendered 1,905 by 17,388 dimensions and nine bounded technical signals with explicit non-behavioral limitations, and permanently deleted both the verified object and an interrupted pending reservation. A fresh browser reload showed both tombstones and 0 console errors or warnings; direct close-out showed two `deleted` asset rows, zero derived profile rows, and zero remaining objects. The journey exposed two control-plane migration defects: the v2 web path still called the removed v1 dashboard route, and strict validation rejected documented `{}` deletion and cancellation commands. NestJS now owns a tenant/RLS-scoped v2 dashboard query and strict response contract; the web client uses version-aware routing; both empty commands carry validation metadata while rejecting supplied fields. Generated OpenAPI/client drift passes. Current regression gates pass 35 API suites / 206 tests, 38 web files / 132 tests, and 7 contract tests; API/web/contracts lint, API/contracts builds, and focused browser screenshots under `output/playwright/` also pass. | OBSERVED | High | This is disposable local evidence, not hosted Supabase proof. Auth and S3 were protocol-shaped loopback harnesses; the S3 object store was process-local, not Supabase Storage, and a forced harness restart created the second pending reservation used to exercise cleanup. The journey used the governed non-representative authored-demo cohort and technical pixel signals only. Cross-tenant browser/HTTP denial, retention expiry, object-delete failure recovery, loading/empty states, human screen-reader review, hosted TLS/CORS/Storage, telemetry, backup/restore, deployment, rights-cleared audience construction, held-out human/outcome validation, external-provider admission, behavioral accuracy, Predikta equivalence, and production readiness remain open. All production flags remain off; no customer data, commit, push, or production mutation occurred. | Plan 003 M5/M6 partial, ADR-0014, ADR-0015, R-005, R-012, R-023, R-032, R-038 |
| E-5056 | Adversarial NestJS private-asset HTTP proof | 2026-07-30 | A newly initialized PostgreSQL 17 database accepted all 47 migrations, then the actual NestJS application used least-privilege `simula_api`, Redis, a loopback RS256/JWKS authority, and the real AWS-SDK S3 adapter against a bounded process-local S3-shaped server. The opt-in HTTP integration reserves and uploads an exact SHA-256-bound object, proves a foreign tenant receives the same generic 404 on content read and deletion while no object GET occurs and bytes remain, then simulates an ambiguous object-provider deletion that returns 204 while retaining bytes. NestJS detects the retained object, returns 503, and leaves the database row `deletion_requested`; retrying the same command key returns `Idempotent-Replayed: true`, removes the object, and confirms `deleted`. A second available object is advanced beyond retention and returns 404 before any object GET. Default controller tests independently cover expiry-before-storage and same-key deletion recovery. The explicit database suite passes 5/5; the full API suite passes 35 suites / 208 tests, followed by test-source compilation, typecheck, production build, and OpenAPI drift. | OBSERVED | High | This is disposable local adversarial evidence, not hosted Supabase proof. Project/stimulus setup used the real gateway to avoid exhausting unrelated mutation-burst capacity before the asset retries. Auth and S3 were protocol-shaped loopback processes; object bytes were process-local; Redis 8.0.4 was not the pinned production release. The test does not prove cross-tenant browser UX, hosted TLS/CORS/Storage, provider-specific delete semantics, human assistive technology, telemetry, backup/restore, deployment, rights-cleared audience construction, held-out human/outcome validation, behavioral accuracy, Predikta equivalence, or production readiness. All production flags remain off; no customer data, commit, push, deployment, or production mutation occurred. | Plan 003 M5/M6 partial, ADR-0014, ADR-0015, R-005, R-012, R-023, R-032, R-038 |
| E-5057 | Durable NestJS-to-Python BullMQ v2 run proof | 2026-07-30 | A newly initialized UTF-8 PostgreSQL 17 database accepted all 47 migrations, and isolated Redis carried the exact `simula:v2` BullMQ namespace. The actual NestJS application authenticated through a loopback RS256/JWKS authority, wrote organization/project/stimulus/run state through least-privilege `simula_api`, and published the identifier-only schema-v2 job. The pinned Python BullMQ 2.14.0 worker connected only as `simula_worker`; its first delivery received database-authorized `awaiting_confirmation` and moved the job active-to-delayed without creating an attempt. The actual Node outbox dispatcher then claimed the durable legacy outbox identity, proved the exact retained v2 BullMQ job, confirmed dispatch, and claimed zero rows on an immediate second pass. The redelivered Python job claimed the frozen run, executed the deterministic provider, and committed `succeeded`, one attempt, one immutable result, and one dispatched outbox row. Removing and republishing the exact completed queue identity produced a completed duplicate delivery while durable attempt/result counts remained one. The fresh focused test passes 1/1; the complete opt-in Nest database suite passes 3 suites / 6 tests. Regression gates pass 35 API suites / 208 tests, 2 live-Redis Nest suites / 7 tests, 82 worker tests, one pinned Python BullMQ live-Redis test, strict Ruff/mypy, API typecheck/build, generated OpenAPI/contracts, forbidden-claim policy, and secret scan. | OBSERVED | High | This closes the real database/Redis/cross-language confirmation, terminal-result, and duplicate-delivery subset of M3 only. It does not yet prove BullMQ crash/stall recovery, cancellation, poison dispatch, stale lease, Redis loss, organization/global backpressure, or behavioral-engine execution under the v2 transport. PostgreSQL and Redis were isolated local services rather than exact Supabase/pinned production images; Auth was protocol-shaped loopback; the test deliberately requires a fresh disposable database because a completed run spans command- and worker-owned tables. BullMQ dispatcher/worker production admission remains rejected and ARQ remains the rollback path. No hosted service, customer data, external provider, rights-cleared audience, held-out human/outcome validation, behavioral accuracy, Predikta equivalence, deployment, commit, push, or production-readiness claim exists. | Plan 003 M3 partial, ADR-0011, R-027, R-032, R-036 |
| E-5058 | BullMQ queued-cancellation and hard-crash recovery proof | 2026-07-30 | The fresh 47-migration PostgreSQL 17, isolated Redis, actual NestJS application/dispatcher, loopback RS256/JWKS authority, least-privilege `simula_api`/`simula_worker`, and pinned Python BullMQ 2.14.0 boundary now execute three real durable scenarios. The confirmation/result/duplicate scenario remains exactly-once at one attempt and one result. Canceling a queued run through HTTP before dispatcher claim reaches `canceled`, zero attempts, zero results, and terminal outbox code `canceled` while the already-published Python job settles only after four bounded database-authorized confirmation deferrals. In the crash scenario, the Python process exits with code 97 immediately after a successful claim, leaving BullMQ active and the run `running`; after the test expires the database leases and backdates progress by 121 seconds, the real dispatcher reports one recovery, creates and confirms generation 2, and the next Python delivery succeeds. Durable close-out records attempt 1 as `superseded/recovered_stale_dispatch`, outbox generation 1 as `terminal/recovery_replaced`, attempt 2 as `succeeded`, generation 2 as `dispatched`, run attempt count 2, and exactly one immutable result. Focused proof passes 3/3; all three opt-in Nest database suites pass 8/8. Regression gates pass 35 API suites / 208 tests, 2 live-Redis Nest suites / 7 tests, 82 worker tests, one pinned Python BullMQ live-Redis test, strict scoped Ruff/mypy/Prettier, test-source and application typecheck, API build/OpenAPI, generated contracts, forbidden-claim policy, and secret scan. | OBSERVED | High | This closes queued cancellation and hard-process-loss/stale-database-lease recovery subsets only. The lease expiry is deliberately advanced by the test; exact BullMQ stalled-job timer behavior, poison dispatch, Redis loss, organization/global backpressure, running cancellation, behavioral-engine execution under v2, and production admission remain open. PostgreSQL, Redis, and Auth are disposable local boundaries, not hosted Supabase or exact production services. BullMQ production admission remains rejected and ARQ remains the rollback path. No hosted service, customer data, external provider, rights-cleared audience, held-out human/outcome validation, behavioral accuracy, Predikta equivalence, deployment, commit, push, or production-readiness claim exists. | Plan 003 M3 partial, ADR-0011, R-027, R-032, R-036 |
| E-5059 | BullMQ Redis-loss recovery and poison-admission proof | 2026-07-30 | The fresh 47-migration PostgreSQL 17 fixture adds a second isolated Redis server dedicated to failure injection. After a real BullMQ connection is ready, that Redis server is shut down and an independent connection probe confirms refusal. The actual Node dispatcher claims the durable outbox row, but publish and retained-job proof cannot succeed, so the pass reports one claim, zero confirmation, and the database remains `queued/claimed` with dispatch attempt count 1 and zero results. After the test expires only that outbox claim and uses the live main Redis transport, the actual dispatcher claims again, publishes and confirms; the pinned Python BullMQ 2.14.0 worker commits `succeeded`, one execution attempt, one immutable result, and outbox dispatch attempt count 2. A separate run is advanced to an expired tenth dispatch claim: the actual dispatcher reports one poison, atomically records run `failed`, outbox `terminal/dispatch_exhausted`, zero execution attempts/results, global control `disabled/poison_outbox`, and one worker audit. The next authenticated NestJS run command returns `503 queue_backpressure` with `Retry-After: 30`; exact `operator_recovery_verified` re-enable restores admission and writes one operator audit. Focused proof passes 5/5; all three opt-in Nest database suites pass 10/10 and exit without open handles. Regression gates pass 35 API suites / 208 tests, 2 live-Redis Nest suites / 7 tests, 82 worker tests, one pinned Python BullMQ live-Redis test, strict scoped Ruff/mypy/Prettier, test-source and application typecheck, API build/OpenAPI, generated contracts, forbidden-claim policy, and secret scan. | OBSERVED | High | This closes real local Redis-loss retry/convergence and poison-triggered global-admission subsets only. The test advances poison directly to an expired tenth claim instead of inducing ten wall-clock outages. It does not prove exact BullMQ stalled-job timing, running cancellation, count/age/organization saturation thresholds, behavioral-engine execution under v2, hosted dependency behavior, or production admission. Redis/PostgreSQL/Auth are disposable local boundaries and Redis is not the exact pinned hosted release. BullMQ production admission remains rejected and ARQ remains rollback. No hosted service, customer data, external provider, rights-cleared audience, held-out validation, behavioral accuracy, Predikta equivalence, deployment, commit, push, or production-readiness claim exists. | Plan 003 M3 partial, ADR-0011, R-027, R-032, R-036 |
| E-5060 | BullMQ running-cancellation and real private behavioral-engine proof | 2026-07-30 | A newly initialized UTF-8 PostgreSQL 17 database accepted all 48 migrations. The actual NestJS application, loopback RS256/JWKS authority, least-privilege `simula_api` and `simula_worker`, actual outbox dispatcher, isolated Redis/BullMQ namespace, pinned Python BullMQ 2.14.0 worker, and actual private FastAPI service execute two new scenarios. For running cancellation, the worker claims and heartbeats before a deliberately paused provider; authenticated HTTP cancellation reaches `cancel_requested`; releasing the provider settles the attempt `canceled/canceled_by_user`, run `canceled`, outbox `terminal/canceled`, and zero deterministic or behavioral result artifacts. For behavioral execution, the command resolves active non-representative audience version `00000000-0000-4000-8000-0000000000d2`, writes the canonical queued `created` event, dispatches one schema-v2 identifier job, executes the admitted deterministic tiered engine, and atomically persists one succeeded attempt, one behavioral summary, one private canonical payload, one provider receipt, normalized evidence/public summaries, and a checksum-valid artifact. The authenticated result route returns `experimental`, provider `deterministic_tiered`, and 10 provider calls. Clean execution exposed and fixed retired audience selection, a non-admitted event reason, non-canonical artifact key ordering, missing completion-owner summary/FK-lock authority, revoked normalization-owner execution, missing action-event FK-lock authority, and a missing worker telemetry operation allowlist. Focused BullMQ proof passes 7/7; all three opt-in database suites pass 12/12. Regression gates pass API 208/208, Nest live-Redis 7/7, Python domain/worker/engine 250/250, two cross-language private HTTP/BullMQ integrations, scoped Ruff/mypy/Prettier, API typecheck/build/OpenAPI, generated contracts/contracts build, 159-file forbidden-claim policy, and 1,870-file secret scan. | OBSERVED | High | This is deterministic experimental local evidence only. Docker Desktop was stopped and the disposable PostgreSQL package set lacked pgTAP, so the updated M4/M5/M6 pgTAP files and Docker-bound generated database-type gate did not run; the complete migration chain and live database path did run. Exact BullMQ stalled timing, count/age/organization saturation, behavioral duplicate/crash/replay, deletion cascade, hosted Supabase/Redis/FastAPI behavior, external-provider admission, rights-cleared population data, held-out human/outcome validation, predictive validity, deployment, and production admission remain open. BullMQ production flags remain rejected and ARQ remains rollback. No customer data, behavioral accuracy, population representation, Predikta equivalence, commit, push, deployment, or production-readiness claim exists. | Plan 003 M3/M4 partial, ADR-0011, ADR-0012, R-027, R-032, R-033, R-036, R-038 |

| E-5061 | Behavioral replay, crash recovery, and deletion-cascade proof | 2026-07-30 | A newly initialized UTF-8 PostgreSQL 17 database accepted all 49 migrations. The actual authenticated NestJS behavioral command replays the original accepted `queued` snapshot under the exact idempotency key and same run identity after the run has succeeded. Removing the retained completed BullMQ job and republishing the exact schema-v2 identity produces a completed duplicate delivery with one attempt, one outbox generation, one canonical result, one payload, one receipt, one context graph/fleet/public-summary set, 10 action/memory/public-agent rows, and no extra durable work. A separate pinned Python worker exits with code 97 after a behavioral claim; after the test advances the expired database leases and 121-second progress threshold, the actual dispatcher replaces generation 1 and the real private FastAPI engine succeeds in generation 2. Durable evidence records attempts `superseded/recovered_stale_dispatch` then `succeeded`, outbox generations `terminal/recovery_replaced` then `dispatched`, and exactly one checksum-valid result/payload/receipt. Deleting the completed behavioral run initially failed because the non-login owner of `api.behavioral_run_results` lacked `DELETE` for the foreign-key cascade. Migration `20260730170000` grants only that owner capability; the runtime `simula_worker` role still has no direct delete privilege. The rerun deletes the run and proves zero remaining run, attempt, event, outbox, canonical artifact, context, fleet, action, memory, report-evidence, and public-summary rows. Focused BullMQ proof passes 8/8; all opt-in database suites pass 13/13. Regression gates pass API 208/208, Nest live-Redis 7/7, Python 387 passed with 2 expected Windows skips, contracts 7/7, test-source compilation, API/contracts builds, OpenAPI/generated-contract drift, scoped formatting/lint, 159-file forbidden-claim policy, and 1,871-file secret scan. | OBSERVED | High | This closes local deterministic behavioral replay, forced duplicate delivery, hard-process-loss recovery, and relational cascade proof only. Lease/progress time is advanced by the test; exact BullMQ stalled-job wall-clock timing and count/age/organization saturation remain open. Docker was stopped and the disposable PostgreSQL package set lacked pgTAP, so pgTAP and generated database-type gates did not run. PostgreSQL, Redis, Auth, and FastAPI are disposable local boundaries, not hosted services. BullMQ production flags remain rejected and ARQ remains rollback. User-facing deletion orchestration, object/cache cleanup, hosted dependencies, external-provider admission, rights-cleared population data, held-out human/outcome validation, predictive validity, deployment, and production admission remain open. No customer data, behavioral accuracy, population representation, Predikta equivalence, commit, push, deployment, or production-readiness claim exists. | Plan 003 M3/M4/M5 partial, ADR-0011, ADR-0012, R-027, R-032, R-033, R-036, R-038 |
| E-5062 | BullMQ production-stall timing and saturation-boundary proof | 2026-07-30 | A newly initialized UTF-8 PostgreSQL 17 database accepted all 50 migrations. Migration `20260730180000` adds a `simula_worker`-only security-definer boundary for durable transient BullMQ pressure; a real `simula_api` connection is rejected with SQLSTATE `42501`. Authenticated run creation now rejects a worker-observed ready depth of at least 100, oldest ready age of at least 60 seconds, or Redis memory of at least 80%, while an observed healthy snapshot clears only that transient reason and leaves the existing operator-verified critical latch intact. Real BullMQ jobs prove depth 100 returns `503 queue_backpressure` while 99 reopens, and a 61-second ready job blocks while a 30-second job reopens. Unit tests prove live Redis memory parsing and dispatcher wiring; the real SQL/HTTP boundary closes/reopens at bounded memory inputs 80/79.9%. The organization pending boundary accepts run 20, rejects run 21 as `429 quota_exceeded`, preserves exact replay, and concurrent worker-role v2 claims produce three `claimed` plus one `organization_capacity`; after one lease expires, the fourth claim succeeds with four total durable attempts but no more than three active leases. A hard-crashed pinned Python BullMQ worker leaves generation 1 active. A second worker using the production `lockDuration=30000`, `stalledInterval=30000`, and `maxStalledCount=1` redelivers and settles that same job on `attempts_started=2` only within the asserted 27-70 second wall-clock window; PostgreSQL remains `running`, attempt count 1, result count 0 until its separate stale-lease threshold is advanced, after which dispatcher recovery confirms generation 2 and one result succeeds. Focused proof passes 11/11; all database suites pass 16/16. Regression gates pass API 211/211, Nest live-Redis 7/7, Python 387 passed with 2 expected Windows skips, contracts 7/7, scoped formatting/Ruff/mypy, test-source/application types, API/contracts builds, OpenAPI/generated-contract drift, 159-file forbidden-claim policy, and 1,872-file secret scan. | OBSERVED | High | This closes the remaining local M3 exact stall-timer and count/age/organization saturation subset. The memory 80/79.9 admission boundary uses a bounded direct operational input; a live Redis configured near `maxmemory` was not induced. PostgreSQL/Redis/Auth are disposable local services, not hosted Supabase or production Redis. Docker Linux remains unavailable, so pgTAP and generated database-type gates did not run. Production-shaped multi-replica load, hosted dependencies, external-provider admission, rights-cleared population data, held-out human/outcome validation, predictive validity, deployment, and production admission remain open. BullMQ production flags remain rejected and ARQ remains rollback. No customer data, behavioral accuracy, population representation, Predikta equivalence, commit, push, deployment, or production-readiness claim exists. | Plan 003 M3 local subset, ADR-0011, R-021, R-027, R-032, R-036 |
| E-5063 | BullMQ two-dispatcher/two-worker 30-run load proof | 2026-07-30 | A newly initialized UTF-8 PostgreSQL 17 database accepted all 50 migrations. The actual NestJS v2 boundary creates 30 authenticated runs across 30 separate tenant organizations. Two distinct `RunOutboxDispatcher` replicas use separate `simula_worker` PostgreSQL pools and separate BullMQ Redis clients; both claim and confirm work, their combined passes confirm all 30 exact outbox identities once, and both return zero-work passes afterward. Two distinct synchronized Python worker processes each run the pinned BullMQ 2.14.0 production adapter with concurrency 4; both claim at least one job, their claim sets are disjoint, and their union is the exact 30-job set. Every retained BullMQ job reaches `completed` with `attemptsStarted=1`. PostgreSQL records 30 succeeded runs, 30 succeeded attempts, 30 dispatched outboxes with aggregate dispatch-attempt count 30, and 30 immutable results. Final clean full-suite measurement reports attempt-duration p95 `0.161628s`, maximum `0.174258s`, and creation-to-last-terminal fixture span `6.049773s`, below the existing 10-second deterministic p95 budget. Focused load proof passes 1/1; all database suites pass 17/17. Regression gates pass API 211/211, Nest live-Redis 7/7, Python 387 passed with 2 expected Windows skips, contracts 7/7, scoped formatting/Ruff/mypy, test-source/application types, API/contracts builds, and OpenAPI/generated-contract drift. | OBSERVED | High | This closes local M3 two-replica deterministic load only. One run per organization avoids conflating this proof with the same-organization three-slot saturation already covered by E-5062. Disposable PostgreSQL/Redis/Auth and zero-cost deterministic providers do not reproduce hosted Supabase/Railway network behavior, provider latency/cost, maximum behavioral workloads, autoscaling, regional failure, or a staging rollback. Exact Supabase-container pgTAP/generated database types, hosted dependencies, no-dual-consume cutover drill, external-provider admission, rights-cleared population data, held-out validation, deployment, and production admission remain open. Production BullMQ flags remain rejected and ARQ remains rollback. No customer data, behavioral accuracy, population representation, Predikta equivalence, commit, push, deployment, or production-readiness claim exists. | Plan 003 M3 local load subset, ADR-0011, R-027, R-032, R-036 |
| E-5064 | Durable ARQ/BullMQ ownership fence and local cutover proof | 2026-07-30 | A newly initialized UTF-8 PostgreSQL 17 database accepted all 51 migrations. Migration `20260730190000` creates a forced-RLS singleton active transport, defaulting to ARQ; a transaction-scoped shared advisory lock fences worker claims while the audited operator switch takes the exclusive form of the same lock. Switching rejects unless new-run admission is disabled and zero non-terminal runs plus zero pending/claimed outboxes remain. Only `simula_operator` can read/switch through named functions; worker access is limited to ownership assertion and transport-specific claims; `simula_api` has no switch authority. The Node dispatcher requires BullMQ ownership before cancellation, recovery, outbox, or execution mutation, while Python ARQ dispatch requires ARQ ownership before mutation. A clean fixture switches drained ARQ to BullMQ, rejects rollback while a queued run exists, completes one BullMQ result, switches to ARQ, rejects the stale BullMQ dispatcher and v2 claims before mutation, then recuts to BullMQ. Final state has exactly one attempt/result and three transport-change audits. Focused transport tests pass 2/2 and the complete opt-in database suites pass 19/19. Regression gates pass API 213/213, Nest live-Redis 7/7, Python 396 passed with 2 expected Windows skips, contracts 7/7, focused CLI/runtime tests 16/16, scoped Prettier/Ruff/mypy, application/test-source types, API/contracts builds, OpenAPI/generated-contract drift, the 159-file forbidden-claim policy, and the 1,876-file secret baseline. | OBSERVED | High | This proves a disposable local durable-fence/cutover subset, not the complete hosted staging drill: it does not execute a terminal result through an ARQ consumer after rollback. WSL virtualization became unavailable after the green suite, preventing a redundant final SQL snapshot and normal fixture shutdown; the test assertions had already verified the stated state. Windows still exposed the disposable PostgreSQL/Redis listeners on ports 54325/6387 but denied process termination; workspace fixture scripts were removed and the unrelated Redis on 6379 remained healthy. Docker Linux was unavailable throughout, so the authored pgTAP assertions and generated database-type gate did not run. Hosted dependencies, autoscaling/failure load, external-provider admission, rights-cleared population data, held-out validation, predictive validity, deployment, and production admission remain open. No customer data, behavioral accuracy, population representation, Predikta equivalence, commit, push, deployment, or production-readiness claim exists. | Plan 003 M3 local cutover subset, ADR-0011, R-027, R-032, R-036 |
| E-5065 | Authenticated production-Next cross-tenant browser proof | 2026-07-30 | A reusable verifier builds the actual NestJS API and Next.js standalone output, assembles its static and public assets, then starts a Supabase-shaped password/Auth/JWKS boundary, actual NestJS v2 application, least-privilege `simula_api` PostgreSQL/RLS connection, isolated Redis namespace, and production Next standalone server. The browser signs in as seeded subject B, safely records the sign-in, and receives `200` with `{items: [], next_cursor: null}` while the organization loading and empty states render, even though subject A owns a real organization/project in the same database. Direct navigation to A's project makes the actual API return generic `404 not_found`; visible text contains none of the foreign organization ID/name, project ID/name, or objective. Both states pass Axe with zero violations. The first run exposed a missing h1 on the denied-project state and a missing `apps/web/public` copy in the standalone runtime; both are corrected, and a deployment test guards the public-asset copy. Final gates pass Playwright 1/1, API 213/213, web 132/132, Nest live-Redis 7/7, deployment tests 9/9, API/web application and test-source types, web lint, production builds, OpenAPI/generated-contract drift, scoped formatting/lint, the 159-file forbidden-claim policy, and the 1,880-file secret baseline. Teardown deletes the organization, resets the temporary API role password to null, closes Auth/API/web listeners, and preserves the unrelated Redis, which returns PONG. | OBSERVED | High | This closes the local M2 authenticated cross-tenant HTTP/browser subset and the organization loading/empty plus project-denial UX subset only. Auth implements the current Supabase SSR password/session/JWKS protocol shape but is not hosted Supabase Auth; PostgreSQL/Redis are disposable local services; no hosted TLS/CORS/provider behavior or human screen-reader session was exercised. Exact Supabase reset/lint/generated database types, hosted Auth/Storage/TLS, remaining workflow loading/empty states, human assistive-technology evidence, deployment, and production admission remain open. No customer data, behavioral accuracy, population representation, predictive validity, Predikta equivalence, commit, push, deployment, or production-readiness claim exists. | Plan 003 M2/M6 local subset, R-013, R-023, R-032 |
| E-5066 | Campaign-lab async-state and cross-tenant denial completion | 2026-07-30 | A bounded audit confirms run/result surfaces already expose a stable level-one heading, loading state, error alert, and refresh path. The project directory now marks its list busy, distinguishes initial failure from a real empty collection, and retries without false empty copy. The organization dashboard renders a generic `Dashboard unavailable` level-one boundary and clears stale load error after successful retry. The methodology lab retries initial load; explains absent audience, configuration, stimulus, and variant prerequisites; reports no compatible comparison explicitly; and replaces blank reaction/risk/rationale/guidance/limitations/audit lists with truthful empty copy. Focused component coverage passes 10/10. The production standalone browser proof now visits a foreign organization dashboard, foreign project directory, and foreign project through actual Supabase-shaped cookies/JWT, NestJS, least-privilege PostgreSQL/RLS, and isolated Redis. Every API response is a generic 404, visible text contains none of the foreign organization ID/name, project ID/name/objective, and all organization-empty plus three denial states pass Axe with zero violations. The first extended run exposed duplicate accessible `Projects` landmarks; the redundant hero landmark label was removed without visual change. Final gates pass Playwright 1/1, web 39/39 files and 138/138 tests, API 35/35 suites and 213/213 tests, API/web TypeScript and web lint, production Next/API builds, contracts build/drift, OpenAPI drift, scoped formatting, the 160-file forbidden-claim policy, and the 1,881-file secret baseline. Teardown closes Auth/API/web ports, deletes the fixture organization, restores the API role password to null, and leaves Redis returning PONG. | OBSERVED | High | This closes the remaining locally audited automated M6 loading/empty/error-state subset only. Auth is a protocol-shaped local boundary, not hosted Supabase Auth; PostgreSQL/Redis are disposable local services. Exact Supabase reset/lint/generated database types remain unavailable on this host; no human screen-reader session, full responsive/visual review, hosted Auth/Storage/TLS, deployment, or production admission ran. No customer data, behavioral accuracy, population representation, predictive validity, Predikta equivalence, commit, push, deployment, or production-readiness claim exists. | Plan 003 M2/M6 local subset, R-013, R-023, R-032 |
| E-5067 | Desktop/mobile authenticated responsive and visual proof | 2026-07-30 | The production-build M2 browser verifier now runs the same authenticated cross-tenant journey under desktop Chrome and a Pixel-class mobile profile. Each profile signs in through the Supabase-shaped cookie/JWT/JWKS boundary, reaches actual NestJS with least-privilege PostgreSQL/RLS and Redis, verifies the membership-free organization empty state, and visits generic foreign organization-dashboard, project-directory, and project denials. All four states per profile require a non-empty streamed document title, zero Axe violations, no browser page errors, only the exact intentional generic 404 responses/console records, no fixture identifier/name/objective disclosure, and no more than one CSS pixel of horizontal overflow. The 2/2 matrix emits eight stable full-page screenshots under `output/playwright/m2-*.png`; all eight were manually inspected and show readable, navigable, unclipped desktop/mobile layouts. A repeated mobile run caught a test-timing race where Axe could inspect before Next streamed its title; explicit title readiness now makes the scan deterministic. Screenshot capture also resets smooth scrolling for stable top-of-page evidence. Teardown closes all three E2E listeners, deletes the fixture organization, restores the API role password to null, and leaves Redis returning `PONG`. | OBSERVED | High | This is automated local responsive/visual evidence, not a human screen-reader session, physical-device test, or hosted Supabase proof. Auth is protocol-shaped and PostgreSQL/Redis are disposable local services. Exact Supabase reset/lint/generated database types, hosted Auth/Storage/TLS, human assistive-technology review, deployment, and production admission remain open. No customer data, behavioral accuracy, population representation, predictive validity, Predikta equivalence, commit, push, deployment, or production-readiness claim exists. | Plan 003 M2/M6 local responsive subset, R-013, R-023, R-032 |
| E-5068 | Post-rollback ARQ terminal execution and no-dual-consume proof | 2026-07-30 | The existing authenticated rollback integration now switches a drained BullMQ pipeline to ARQ, re-enables admission, creates a second run through the actual NestJS v2 command, and starts a bounded one-shot harness using the production Python `RunDispatcher`, strict ARQ codec/queue adapter, `WorkerDatabase`, `_create_arq_worker`, deterministic provider, and `_close_arq_worker`. The dispatcher claims/confirms exactly one canonical `run:{uuid}:dispatch:1` intent; the real ARQ worker completes it; authenticated run/result reads return `succeeded` and schema 1; PostgreSQL records exactly one attempt and one result. The Nest command also left its immediate schema-v2 BullMQ delivery retained. After disabling admission, drained recutover to BullMQ, and executing that retained delivery, BullMQ completes it as a database-authorized no-op and PostgreSQL still records one attempt/result. TDD first failed on the absent helper, then exposed a real production defect: `WorkerDatabase` emitted `require_queue_transport` telemetry but that fixed-cardinality operation was missing from `WorkerTelemetry`'s allowlist, causing ARQ dispatch to fail before claiming work. A focused red/green test adds the label, and the dispatcher protocol now accepts the exact ARQ/BullMQ literal domain. A clean 51-migration PostgreSQL 17 fixture with independent Redis 6387 main and disposable 6388 outage servers passes all 14 BullMQ/ARQ database cases, including real Redis loss/recovery, poison latch, crash/stall recovery, 30-run two-replica load, and the new rollback path. Regression gates pass API 35/35 suites and 213/213 tests, Python 396 passed with 2 expected Windows skips, API build/typecheck, generated-contract drift, focused Prettier/Ruff/mypy, the 160-file claim policy, and the 1,882-file secret baseline. Final SQL/port proof shows ARQ active, admission enabled, zero named fixture organizations/nonterminal runs/open outboxes/operator fixture audits, null `simula_api`/`simula_worker` passwords, Redis 6387 `PONG`, outage-only 6388 closed, and unrelated 6379 still listening. | OBSERVED | High | This closes the missing local ARQ-consumer terminal subset and local retained-delivery no-dual-durable-write subset only. It is not hosted Supabase/Railway or the complete staged release-identity/alert/rollback drill. Docker Linux remains unavailable, so authored pgTAP and generated database-type gates did not run. Full-repo format/lint/mypy remain red only on unrelated existing `apps/admin/next-env.d.ts`, `scripts/generate_user_guide_pdf.py`, and `packages/simula-core/tests/test_visual_analysis.py`; scoped changed-file gates pass. Hosted dependencies, autoscaling/regional failure load, external-provider admission, rights-cleared data, held-out human/outcome validation, predictive validity, deployment, and production admission remain open. No customer data, behavioral accuracy, population representation, Predikta equivalence, commit, push, deployment, or production-readiness claim exists. | Plan 003 M3 local rollback subset, ADR-0011, R-021, R-027, R-032, R-036 |
| E-5069 | Whole-repository quality, PDF, and dependency-security recovery | 2026-07-30 | The stale root-gate blockers recorded by E-5068 are closed without changing product behavior or guide copy. The workspace now declares ReportLab 4.5.1 plus matching `types-reportlab` 4.5.1.20260712; strict guide callback/table-style typing passes; Ruff retains only a file-scoped `E501` exception for long copy literals; generated `apps/admin/next-env.d.ts` is Prettier-clean; and `ImageOps` is explicitly re-exported for the visual-analysis failure-path test. The first SCA run found current Pillow 12.1.0 advisories, so the existing exact pin was upgraded to Pillow 12.3.0. Focused visual tests pass 9/9. Final `pnpm check` exits 0: 130 Python files format cleanly; all TypeScript/ESLint and Ruff lint passes; mypy checks 130 sources; contracts pass 7/7, admin 2/2, NestJS API 213/213, web 138/138, and Python 396 with 2 expected Windows-only skips; all four production builds, generated OpenAPI/client drift, the 160-file forbidden-claim policy, and the 1,882-file secret baseline pass. Separate `pnpm security:sca` exits 0: Python audit reports zero known vulnerabilities and npm reports only the pre-existing documented dev-only GHSA exception with a 2026-08-29 review date. The guide regenerates to 15 A4 pages, 13 bookmarks, non-empty extracted text on every page, correct title/Third Code author metadata, explicit non-human-study language, and zero nonbreaking hyphens. A Poppler all-page contact sheet plus dense pages were visually inspected with no clipping, overlap, broken tables, or unreadable glyphs. | OBSERVED | High | Local source, artifact, and dependency evidence only. The npm result includes the existing scoped dev-only GHSA exception; it is not a claim of zero advisories. Docker Linux remains unavailable, so exact Supabase-container reset/lint/pgTAP/generated database types remain open. No hosted Supabase/Railway/Vercel runtime, human screen-reader session, enforceable required-check policy, external provider, rights-cleared audience data, held-out human/outcome validation, predictive validity, deployment, commit, push, Predikta equivalence, or production-readiness claim exists. | Plan 003 cross-cutting quality gate, R-021, R-025, R-033 |

| E-5070 | Durable owner-triggered organization deletion orchestration | 2026-07-30 | Migration `20260730200000` adds a forced-RLS durable deletion request/tombstone outside the organization cascade. The owner-only request command requires the exact organization name, rejects nonterminal runs, persists a bounded manifest before disabling the organization, and makes membership/role authorization fail closed while deletion is pending. The NestJS v2 command then deletes and verifies every private object, removes and verifies all three possible BullMQ generations for every run, scans/deletes/verifies organization-scoped Redis rate and idempotency keys, and only then confirms the PostgreSQL cascade; same-payload retries resume the durable request while changed payloads fail. Direct least-privilege PostgreSQL 17 adversarial proof covers manifest capture, disabled-workspace command freeze, changed-request rejection, same-request replay, active-run rejection, complete relational cascade, minimized completed tombstone, and replay after completion. Live Redis and BullMQ integration proves target organization keys/jobs disappear while unrelated-tenant keys remain. Production Next plus Supabase-shaped Auth/JWT/JWKS, actual NestJS v2, least-privilege PostgreSQL/RLS, Redis, and BullMQ completes a disposable zero-asset/zero-run owner deletion: wrong-name confirmation is blocked, the command returns completed, the deleted workspace disappears, Axe reports zero violations, no horizontal overflow or unexpected page/console errors occurs, and both confirmation/completion screenshots were visually inspected. Final `pnpm check` exits 0 with contracts 7/7, admin 2/2, API 36 suites/223 tests, web 39 files/140 tests, Python 396 passed plus 2 expected Windows skips, all four builds, and generated OpenAPI/client/claim/secret gates; the full browser matrix passes 3 scenarios with one intentional mobile duplicate skip. | OBSERVED | High | Local disposable evidence only. Docker/local Supabase was unavailable, so exact reset/lint/pgTAP and generated database types did not run. The browser deletion fixture intentionally contained no stored objects or queued runs; three unrelated legacy v1 owner reads were isolated with empty route fixtures. Real S3 adapter failure/retry and object absence are covered below the browser boundary, but hosted Supabase Storage/Railway Redis/BullMQ, a populated-object/job browser deletion, automatic recovery of abandoned pending requests, backup-expiry propagation, and staging/production proof remain open. Human screen-reader evidence, required-check governance, full application-compatible restore, external-provider/data admission, held-out validation, deployment, and every behavioral-accuracy, population-representation, predictive-validity, Predikta-equivalence, or production-readiness claim remain open. | Plan 002 M6 local deletion closure; Plan 003 M2/M5/M6 local subset; R-013, R-023, R-024, R-038 |
| E-5071 | Truthful Pillow 12.3 visual provenance and audit-policy regression closure | 2026-07-30 | Post-E-5070 review found that the dependency upgrade recorded in E-5069 still emitted `pillow-12.1.0` in newly generated technical image profiles. It also found that E-5070's complete phase-4 audit-policy rewrite had accidentally omitted `stimulus_visual_profile.created`, causing the real visual-profile database integration to fail closed at audit insertion. Migration `20260730210000` preserves historical 12.1 profile rows, admits 12.3 rows, requires every new database command to carry `pillow-12.3.0`, and restores both visual-profile and organization-deletion audit actions. The Python engine now emits the installed 12.3 identity; Nest/OpenAPI/generated TypeScript and web parsing accept historical 12.1 plus current 12.3 reads while the creation engine accepts only current 12.3. A transactional replay of the complete migration on the disposable PostgreSQL 17 chain verifies the two-version constraint, current function body, `simula_command_owner` ownership, and both audit actions. The previously failing least-privilege gateway integration now passes 4/4, including create/replay/tenant isolation/audit/retirement. Focused gates pass 11 Python/restore, 9 API, 8 web, and 7 contract tests. Final `pnpm check` exits 0 with 130-file Python format/type coverage, contracts 7/7, admin 2/2, API 223/223, web 141/141, Python 396 passed plus 2 expected Windows skips, all four production builds, generated drift, 160-file claims, and 1,886-file secrets; `git diff --check` passes. | OBSERVED | High | Local source/disposable-database evidence only. Docker/local Supabase remains unavailable, so the authored 20-assertion visual pgTAP file, exact reset/lint, and generated database types did not run. No hosted migration was applied and no hosted asset/profile was created. Historical 12.1 profiles remain readable by design; this does not retrospectively change their runtime provenance. Semantic/behavioral visual inference, real multimodal providers, rights-cleared audiences, held-out human/outcome validation, predictive validity, deployment, Predikta equivalence, and production readiness remain absent. | Plan 003 M5/M6 provenance correction; ADR-0015; R-025, R-038 |
| E-5072 | Crash-safe pending organization-deletion recovery | 2026-07-30 | Migration `20260730220000` adds a forced-RLS external-resource ledger seeded from every durable pending deletion manifest. Worker-only security-definer commands lease at most 50 due cache, run, and storage resources with `SKIP LOCKED`, 15-minute claim tokens, ten-attempt ceilings, and bounded 5-300 second retry backoff; completion requires the current unexpired token, releases persist only fixed safe error codes, and finalization is impossible until every ledger row is complete. The dispatcher reconciles at a fixed 30-second cadence using the existing private S3 adapter with post-delete absence checks, exact BullMQ run-generation removal/proof, and exact Redis organization-key patterns with a second absence scan. A PostgreSQL 17 adversarial transaction proves exact cache/storage seeding, API denial, first-pass partial completion, duplicate-completion no-op, persisted safe error/backoff, blocked early finalization, second-attempt completion, one guarded cascade, resource-ledger purge, and minimized tombstone; the pre-existing owner-deletion adversarial transaction and least-privilege gateway integration remain green. Live Redis/BullMQ integration passes 10/10 and proves all five organization-bound rate/idempotency patterns disappear while unrelated-tenant and UUID-colliding user keys remain. Exact live table, policy, grant, and worker-function catalogs match the foundation assertions; 17 focused pgTAP assertions are authored. Final `pnpm check` exits 0 with API 37 suites/230 tests, web 39 files/141 tests, contracts 7/7, admin 2/2, Python 396 passed plus 2 expected Windows skips, all four builds, generated drift, 160-file claim policy, and 1,899-file secret baseline. | OBSERVED | High | Local disposable evidence only. The database proof models a failed pass and later retry transactionally; it does not kill and restart a deployed dispatcher. Redis/BullMQ are live local dependencies, while storage deletion is adapter-level unit proof rather than a populated real S3 recovery pass. Docker/local Supabase remains unavailable, so exact clean reset/lint, the 17 authored pgTAP assertions, and generated database types did not run. No hosted Supabase Storage/Railway recovery, backup-expiry propagation, staging/production execution, deployment, behavioral accuracy, population representation, predictive validity, Predikta equivalence, or production readiness is established. | Plan 003 M2/M3/M5/M6 local recovery subset; R-023, R-024, R-032, R-038 |
| E-5073 | Fail-closed hosted-runtime admission and signed-release preflight | 2026-07-30 | Migration `20260730230000` adds worker-owned `private.runtime_schema_readiness()` with API/worker-only execute access, exact compiled-head reporting, and forced-RLS catalog verification; it also advances the observability snapshot head. NestJS control plane, singleton dispatcher, FastAPI rollback API, Python worker, and private engine now require that head in deployed environments. Production additionally requires exact admission enablement, one lowercase UUIDv4 rollout ID, deterministic archive SHA-256, Sigstore-bundle SHA-256, and the exact SIMULA GitHub Actions run URL. The dispatcher reuses the non-root control-plane image with a dedicated entry point, zero deployment overlap, and liveness plus pass-staleness readiness. A disposable PostgreSQL 17 chain accepted the migration and direct least-privilege catalog checks proved the head, forced RLS, owner, security-definer configuration, and API/worker-only grants. Because GitHub artifact attestations for this private repository require an unavailable Enterprise plan, the tag-only workflow now uses pinned `sigstore/gh-action-sigstore-python` keyless signing, exact `github.workflow_ref` and GitHub Actions issuer verification, and archive/bundle checksums before artifact upload. Exact-toolchain `pnpm check` exits 0: NestJS 39 suites/250 tests, web 39 files/141 tests, Python 414 passed plus 2 expected Windows skips, contracts 7/7, admin 2/2, all builds, generated drift, 161-file claim policy, and 1,913-file secret baseline. SCA exits 0 with the documented ignored npm advisory and zero known Python vulnerabilities; six alerts/eight panels, 10/200/2,000-agent deterministic capacity, and the asset browser fixture pass. The linked Supabase production dry-run lists exactly 18 pending migrations through this head and remote `api`, `private`, and `extensions` lint reports no errors. | OBSERVED | High | The GitHub workflow is authored but unexecuted. No immutable commit, pushed tag, signature, Sigstore bundle, Rekor entry, release artifact, migration push, Railway/Vercel deployment, hosted pgTAP, hosted readiness, restore drill, canary, or production mutation exists. Sigstore's public Rekor service would expose the private repository workflow identity and tag; explicit disclosure plus commit/push/tag authorization is required first. Deterministic authored agents remain non-representative; no rights-cleared population frame, external behavioral provider, held-out human/outcome validation, behavioral accuracy, predictive validity, Predikta equivalence, or production-readiness claim exists. | Plan 003 M7 preflight; R-021, R-025, R-032, R-038 |

Release verification for E-5032: commits `d912b21` and `5ab8f6c` are pushed to `main`; GitHub Actions run `29718093557` completed successfully on exact head `5ab8f6c1b598ca2a57c69ab7ab442333453dba4b` with all four named jobs green. The follow-up commit narrows the existing test-only Gitleaks allowance to the historical synthetic `rejected_key` fixture; pinned Gitleaks 8.30.1 scanned all 70 reachable commits with no leak.

## Explicit unknowns

- UNKNOWN: Predikta and Netopia private source code, complete datasets/licenses, model stack, prompts, algorithms, weighting, calibration implementation, customer results, and production architecture.
- UNKNOWN: Independent replication of vendor population-scale or general accuracy claims.
- UNKNOWN: Whether available Philippine data supports every needed intersection under suitable rights and acceptable sparse-cell risk.
- UNKNOWN: Achievable SIMULA predictive validity until held-out human or outcome evaluation exists.
- UNKNOWN: Final production legal roles, lawful bases, regions, retention periods, incident duties, and real-provider terms until concrete hosted use, contracts, and providers are fixed. These block production, not the no-egress local prototype.
