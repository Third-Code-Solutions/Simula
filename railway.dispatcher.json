{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/api/Dockerfile",
    "watchPatterns": [
      "apps/api/**",
      "packages/contracts/**",
      "package.json",
      "pnpm-lock.yaml",
      "pnpm-workspace.yaml",
      "railway.dispatcher.json",
      "tsconfig.base.json",
      "turbo.json"
    ]
  },
  "deploy": {
    "drainingSeconds": 30,
    "healthcheckPath": "/health/ready",
    "healthcheckTimeout": 120,
    "overlapSeconds": 0,
    "restartPolicyMaxRetries": 10,
    "restartPolicyType": "ON_FAILURE",
    "startCommand": "node --require ./dist/instrumentation.js dist/dispatcher/main.js"
  }
}
