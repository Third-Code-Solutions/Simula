[project]
name = "simula-core"
version = "0.0.0"
description = "Shared SIMULA runtime and transport primitives"
requires-python = "==3.14.6"
dependencies = [
    "pydantic==2.13.4",
]

[build-system]
requires = ["uv_build==0.11.19"]
build-backend = "uv_build"
